# AI Companion

The complete Flutter/Android project lives in `app/` and is the repository's single source of truth.

- Build: run `.github/workflows/build-apk.yml` or follow `app/docs/BUILDING.md`.
- Handoff: read `app/docs/HANDOFF.md` and `app/docs/PROJECT_TASK_LEDGER.md`.
- Clean Freeze: see `app/docs/CLEAN_FREEZE_v0.31.5.md`.

Historical patch-based promotion files were retired after v0.31.5+47 entered `app/`; Git history remains the recovery source.

