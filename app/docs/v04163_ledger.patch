diff --git a/AI_Companion_当前总账.md b/AI_Companion_当前总账.md
index 75ef5ac..3b02a0f 100644
--- a/AI_Companion_当前总账.md
+++ b/AI_Companion_当前总账.md
@@ -32,28 +32,28 @@
 |---|---|
 | 仓库 | 公开仓库 `catkiss62/ai-companion-build`；完整 Flutter/Android 工程在 `app/` |
 | 持续提交与 APK 授权 | 2026-09-02 用户明确“以后一直允许提交”，并于 2026-09-03 再确认：人机恋项目范围内，可将任务相关源码和文档提交推送到本仓库当前或后续明确的开发分支，并直接执行常规 Actions/APK 创建流程，不再逐批重复询问。此授权不包含合并 `main`、发布正式 Release、删除分支/数据、改变仓库权限或公开密钥/隐私资料；这些仍须单独确认 |
-| 当前开发分支 | `agent/v04162-hybrid-final-fallback`，从 +205 已构建源码与已登记的双通道设计开出；目标 `v0.41.62+206 / schema 60 / Snapshot protocol 6`。恢复 +204 玩游固定 0.5 积分模型别名，但只用于用户选择后的最终可见表达；DeepSeek Key 必填并独占内部工作。同步修复沉浸语言按钮高度，排查非日语 TTS 偶发混入日语，并为 Gemini 增加有条件单次重试与 DeepSeek 兜底 |
+| 当前开发分支 | `agent/v04163-immersive-tts-settings-hardening`，目标 `v0.41.63+207 / schema 60 / Snapshot protocol 6`，运行代码已在本地实现、专项与可运行历史源码门通过，尚待 Actions 真编译；当前可安装基线仍为 +206。+207 不直接猜修日语串音/首次切入缺陷，只增强分段与阶段诊断；实现沉浸中文正文闭合句即入 TTS 队列、温柔音色相对 1.2 倍速、全文旁白/动作固定温柔而对白使用已选音色、DeepSeek kill-switch 与两 Key 路由审计，以及侧栏/“更多”唯一设置入口合并 |
 | 上一运行代码基线 | `agent/v0417-forthright-fiery-personality`，功能 head `58c244a4b08033f403776f1ec31bbece5557506d`；Desire/Moe/主动性状态主干仍沿革自 `agent/v0415-personality-state-diversity` / `494796ef02e369f98e6896bc5acea7185e3c35dd` |
 | 有效构建 head / tree | +206 Actions build head `613096b0fff553c2f069994091dc667953211608`；CI 在检出后以 hash-locked delta 重建大数据库/总账，再以完全相同运行源码通过测试与打包，成功后回写为分支 head `3bbc438b0f1f018bb5f791f5b0832b7a39a03452` / tree `4a5d5a605ce96668ca5fda75c47dc6c97afbd1ae`。公开提交不含用户附件、诊断、备份、密钥、RoBERTa、模型权重、参考音频或 APK Artifact；私有小酒狐声学包与三语前端仅在 Actions 中按固定 SHA 分源恢复、裁剪和实包复核 |
-| App / 数据库 | 当前已构建基线为 `0.41.61+205 / schema 60 / Snapshot protocol 6`。schema 60 仅为沉浸消息增加日语/英语投影正文与分段缓存；中文原文仍是权威内容，Snapshot 继续导出 `immersive_messages` 全列 |
+| App / 数据库 | 当前已构建基线为 `0.41.62+206 / schema 60 / Snapshot protocol 6`；+207 继续保持 schema 60 与 Snapshot protocol 6，不做数据迁移。schema 60 仅为沉浸消息增加日语/英语投影正文与分段缓存；中文原文仍是权威内容，Snapshot 继续导出 `immersive_messages` 全列 |
 | 最终 CI | +206 run [`34588761545`](https://github.com/catkiss62/ai-companion-build/actions/runs/34588761545)（837）完整成功：两项超大文件目标 SHA、两私有载荷固定 SHA 与分源、干净基线、全部源码/历史 validator、Kotlin/JVM、Flutter Analyze、`726/726` Flutter tests、arm64 Release、固定签名、48 项 Genie/Jiuhu/OpenJTalk 实包大小/哈希、ORT byte identity、四独立小酒狐 case、退休语音排除、OpenJTalk ELF 闭包、塔罗、Artifact、Draft 上传及分支回写均通过 |
 | 测试 APK | `AI-Companion-v0.41.62-206-Hybrid-Final-Safe-Drafts-APK.apk`，544,680,678 bytes |
 | APK SHA-256 | `c57664d925d39d1fb4d1368a2a8263e4e6e8bcdab0f9432912771d5e41fc3fdf`；与 CI checksum 和 Draft 资产服务端 digest 一致 |
 | Artifact / Release | Artifact [`10195107821`](https://github.com/catkiss62/ai-companion-build/actions/runs/34588761545/artifacts/10195107821)，ZIP 537,801,712 bytes / digest `a80f363a5bbf89c07c98fa18c46759ae66765f0d3f69fac6fc4be44261718c2a`；[Draft Release](https://github.com/catkiss62/ai-companion-build/releases/tag/untagged-09fa2e8978b350c82cff) 未发布，`main` 未合并 |
 | `main` | 仍停在 v0.38.5 旧基线，未合并 v0.41.x；**不得从 `main` 误判当前项目或作为后续开发基线** |
-| 当前总状态 | +204 小酒狐语音链 `TRUE DEVICE PASSED`；+205 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`，帅 API 不可用且沉浸房间按钮高度异常。+206 当前为 `CI PASSED / APK READY / TRUE DEVICE PENDING`；双通道、兜底、截断草稿、按钮尺寸和英语 TTS 污染防护均已自动化与实包验证，真实玩游扣分/文笔、故障提示、草稿交互和三语听感仍须用户真机确认。动作/神态括号继续只观察，不改 Prompt |
+| 当前总状态 | +204 小酒狐语音链 `TRUE DEVICE PASSED`；+205 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`，帅 API 不可用且沉浸房间按钮高度异常。+206 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`：用户明确反馈重要真机测试已通过，脱敏诊断亦证实版本/迁移、Active Brain、后台通道、Accessibility、通知、悬浮触摸自愈、公网与真实 TTS 播放主链正常；但语言切换矩阵显示日语只在“首次即选日语”的消息链成功，从已播放中文/英语的状态切入日语会失败，诊断有对应 `language=ja` 的 `NullPointerException`，故 TTS 尚不能整体收口。Gemini 失败重试/兜底与截断草稿的专用故障分支本报告无触发证据，不倒写为已验收。动作/神态括号继续只观察，不改 Prompt |
 
 ### 3. 当前下一步任务包（新窗口必须完整接住）
 
 | 字段 | 当前内容 |
 |---|---|
-| 当前下一步 | **+206 真机验收**：覆盖安装新 APK，设置中始终保留 DeepSeek Key；先选择 DeepSeek 确认旧单通道，再选择 Gemini 并额外填写玩游 Key。分别测试普通/沉浸正文文笔与实际每请求 0.5 积分；观察瞬时失败是否最多两次、完全失败是否显示手动关闭前持续的 DeepSeek 兜底提示；用异常断流验证草稿不进入后续上下文，且“重新生成 / 确认回复”二次确认与最终记忆/摘要语义正确。检查沉浸中/日/EN 为紧凑按钮、英语 TTS 不再读入日文污染。已完整提交的历史回复刷新仍延后 |
-| 目标 | 每个正常可见回合通常只产生 1 次玩游固定积分请求，即 0.5 积分/¥0.02；SSE 分片不重复计次。DeepSeek 内部调用继续按其 token 计费。第一版按职责稳定分流，不在请求前用不可靠的 token 猜测器频繁切换模型；先记录各 lane 的请求数/usage，再判断少数超长结构化任务是否值得进入成本路由。Agent 工具由 DeepSeek 规划并在本地执行，Gemini 只根据已经核验的 Outcome 写最终自然正文；沉浸 NSFW 路由和滚动摘要也留在 DeepSeek |
-| 当前证据 | +204 Git 源码确认玩游 endpoint/model 与独立 Key 槽；用户确认该站明示每次请求固定扣 0.5 积分，4 元 100 积分，并非按 token。当前 +205 源码的 `SecureConfig.readApiKey/readEndpoint` 是全局选择，导致主生成、NSFW 路由、每轮经验整合、阶段摘要、自我整理和按需翻译全部跟随 Gemini，正是需要拆分的根因。最终生成通常携带最大的人格/世界书/记忆/历史 Prompt，且文笔质量直接可见，最适合固定价 Gemini；机械 JSON/分类请求更适合 DeepSeek。沉浸截图确认按钮高度异常；07 规则正文已经明确“叙述不加括号”，没有 Prompt 缺项证据 |
+| 当前下一步 | **+207 本地实现完成 / CI PENDING**：把当前源码提交推送并通过 Actions 真编译、Analyze、全量测试、Release/签名/私有 TTS 实包门；若编译或行为测试暴露问题只做窄修。当前实现仅在中文自动朗读时把已经闭合的沉浸句子/对白送入 A2 队列；异常断流立即停止且未闭合尾部不朗读。温柔有效速度=`用户速度×1.2`，全文旁白/动作固定温柔、对白用所选/自动音色，原生播放器在倍率边界排空旧 PCM 后切换。切回 DeepSeek 当刻写入 provider，内部/主动/翻译/摘要继续只读 DeepSeek。侧栏重复分类已删除，“全部设置”切到底部“更多”，其中保留她/你们与完整七域。TTS 诊断新增语言、音色、分段索引/长度、文本 SHA、音素/语义哈希和 native 阶段，不含正文/音频/Key。日语根修、CPU 超频/线程档位仍后排单独真机 A/B |
+| 目标 | 保持 +206 已完成的双通道：每个正常可见回合通常只产生 1 次玩游固定积分请求，DeepSeek 承担内部机械工作；本批额外保证用户切回 DeepSeek 后无需再按保存即可阻断后续 Gemini 请求，主动消息永远只走 DeepSeek。沉浸中文自动朗读从完整响应后启动前移到闭合句流式入队；全文朗读保留动作/对白结构并按温柔/所选音色分流。设置只保留一个权威导航中心，TTS 失败报告能在不泄露正文的前提下区分语言、段落和 native 阶段 |
+| 当前证据 | +206 已把 `SecureConfig.readApiKey/readEndpoint` 固定为 DeepSeek internal lane，普通/沉浸最终正文独立读取 final lane；当前 `ProactiveEngine` 也只读取 DeepSeek。旧版“关闭 Gemini 后仍强行调用”最可能来自提供商下拉只改页面临时值、必须再点保存才落盘，或来自已被 +206 取代的全局路由。TTS 底层已有流式 A2 队列但生产 controller 未接；全文模式当前把所有段落压成单一字符串和单一音色；原生播放速度当前对整条 `AudioTrack` 生效，若要逐段倍率必须在音色/倍率边界等待已写 PCM 排空再切换。用户最新接受 Desire 14 日、Device 21 日/12,000 行的有界清理，不要求本批改变数据库或备份 |
 | 保护与排除 | 不提交或迁移任何 Key；DeepSeek 与玩游 Key 独立安全保存，DeepSeek 缺失时不得只靠 Gemini 启动完整系统。选择 DeepSeek 回复时不得调用玩游；选择 Gemini 但 Key 缺失/请求失败时须保留用户消息并明确回退或报错，不能静默丢轮。不得把 DeepSeek 内部规划正文或机械 JSON显示给用户；不得让 Gemini 重做工具、伪造 Outcome 或把一次回复拆成无界多次固定扣费。玩游 thinking 请求必须采用真实线端方言，不能原样恢复 +204 把 SDK `extra_body` 当 JSON 字段的错误。动作括号本批只观察，不增加重复禁令、不做字符串删括号补丁；已真机通过的 Genie-TTS 小酒狐四音色、三语、停止与单播放链不得回归 |
 | **媒体 Agent 永久合同（P0）** | **任何“她能发送的媒体”都必须同时具备 Agent 自读能力、可执行工具、真实附件 Outcome、来源 provenance 和发送后第一人称历史；只有 UI、随机表达或 Prompt 声称能力都不算完成。** 表情包先实现明确指令 `sticker.send`；后续联网图、相册图也必须分别接入真实工具。失败、无图库、无匹配、下载失败、权限拒绝或事务失效只能如实返回，不得写成已发送 |
 | 实现边界 | D6 新媒体使用 content-addressed blob；聊天消息、相册与表情发送只增加独立引用/引用计数，不再复制相同原图。保存到相册只增加 album ref；缓存页只列未被永久相册引用的聊天媒体，支持多选、全选和合计空间；删除任一引用都不能破坏其余引用。Snapshot protocol 6 必须携带共享 blob，旧 protocol 5 恢复后仍可手动优化，跨设备或缺少原表情包时历史不破图 |
-| 完成判据 | API 测试必须分别截获 internal/final lanes：只填 DeepSeek 时所有旧链可用且零玩游请求；选择 Gemini 后，普通无工具回合为 DeepSeek 路由 + 1 次玩游最终正文 + DeepSeek post-turn，工具回合由 DeepSeek 规划/执行后只把核验 Outcome 交给 1 次玩游最终正文；沉浸为 DeepSeek 路由/摘要 + 玩游正文。需锁定两 Key/端点/模型绝不串线、Gemini `reasoning_content` 可显示、失败回退不重复落消息、外语投影走 DeepSeek，以及 AppBar 三个语言按钮的固定紧凑尺寸。完整回归、Analyze、Release、签名和小酒狐实包门全绿后只写 `APK READY`；真实扣分、文笔、思考摘要和按钮视觉仍须用户真机测试 |
+| 完成判据 | 新测试锁定：选择 DeepSeek 当刻持久化并使下一次普通/沉浸 final lane 立即变为 DeepSeek，主动/内部/翻译/摘要入口绝不读取 Gemini Key；沉浸中文流在首个闭合句后开始生成且完成后不整段重播，异常断流停止并不朗读未闭合尾部；温柔有效倍率为全局速度×1.2，全文动作/旁白固定温柔、对白使用所选/自动音色，倍率边界不污染相邻音频；脱敏诊断包含语言/分段/哈希/native 阶段而不含正文；底部“更多”可达全部旧、新设置而侧栏不再重复四个分类。完整回归、Analyze、Release、签名和小酒狐实包门全绿后只写 `APK READY`；延迟、发热、日语首次切入、单段日语听感和真实 Gemini kill-switch 仍须真机验证 |
 | 直接详细入口 | 世界书：`app/docs/WORLDBOOK_2D_PROVENANCE_ROLEPLAY_V04138.md`；造梗来源：`app/lib/core/ai/dialogue_expression_plan.dart`、`app/lib/core/ai/prompt_builder.dart`；媒体合同：`app/docs/SUBJECTIVITY_LIFELIKENESS_AUDIT_2026-09-08.md` 与 `app/docs/STICKER_SEMANTICS_GALLERY_INTEROP_v0.41.51.md`；Snapshot：`app/lib/core/sync/snapshot_service.dart` |
 
 ### 4. 当前任务完成后的后续导航（只导航，不提前展开）
@@ -90,7 +90,25 @@
 
 ## 近期详细记录与全局索引（按需检索）
 
-### 2026-09-11 v0.41.62+206 双通道最终表达、兜底与安全截断草稿（CI PASSED / APK READY / TRUE DEVICE PENDING）
+### 2026-09-11 v0.41.63+207 沉浸低延迟朗读、音色语义、设置归一与路由硬化（PREWORK / IMPLEMENTING）
+
+1. 用户接受当前运行记录的有界清理事实：`desire_events` 写入时删除 14 天以前记录，`device_events` 由维护任务按 21 天且最多 12,000 行裁剪；短时间高频导致备份暂时增大可以接受，只要求冗余不会永久无限增长。故 +207 不改变 schema、不清空历史、不新增删除按钮，也不把参与近期设备情境判断的事件与纯诊断事件误用同一删除策略；后续只有出现无界增长或备份负担的真机证据才重开。
+2. 本批确定交付沉浸中文流式 TTS：仅当开启自动朗读且当前语言为中文时，在 LLM SSE 中按结构累计，首个完整句或闭合对白一旦形成即送入现有有界生成/播放队列，不等整条正文完成。日语/英语继续等完整中文正文后做一次 DeepSeek 投影，避免按分片重复请求和翻译漂移。正常完成后不得再次整段 `playText`；停止、失败或异常断流立即取消本轮队列，未闭合尾部不生成，截断草稿仍由 +206 的“重新生成/确认回复”决定是否进入上下文与摘要。
+3. 全文朗读从单一扁平字符串升级为带语义的朗读单元：`ChatSegmentCodec`/沉浸段落判定产生 dialogue 与 narration/action；全文模式下旁白和动作永远用“温柔”候选，对话 `「」` 使用用户所选音色或自动情绪音色；仅对白模式继续只使用已选/自动音色。不得按字体颜色反推语义，避免主题或渲染变化破坏 TTS。
+4. “温柔”候选增加固定相对倍率 1.2：最终有效速度=`clamp(用户速度×1.2, 0.5, 2.0)`，其他候选仍为用户速度。设置页继续显示用户的全局倍率，因此 1.0 时温柔实际 1.2，1.2 时温柔实际 1.44；不把存量 setting 自动改写为 1.2。原生播放必须在不同有效倍率边界等待已缓冲 PCM 排空后再切换 `PlaybackParams`，不能让后一个动作段的速度回溯污染已经写入的对白；同倍率相邻片段继续无缝写入。
+5. +206 源码审计已经确认双 Key 主结构正确：legacy `readApiKey/readEndpoint` 固定 DeepSeek，final lane 才会按选择读取玩游；`ProactiveEngine` 直接读取 DeepSeek，因此当前主动消息不会调用 Gemini。发现的真实 UX 风险是提供商下拉只修改页面临时状态，用户切回 DeepSeek 后如果未再点“保存”，持久 provider 仍可能是 Gemini。+207 把“选择 DeepSeek”做成立即落盘 kill-switch；选择 Gemini 仍需填写独立 Key 并明确保存，防止误触发固定积分请求。补静态/单元门覆盖普通、沉浸、主动、Agent、翻译、摘要与 post-turn，不允许内部调用读取 final Key。
+6. TTS 日语问题本批只增强取证，不猜修：诊断按不泄露原则增加请求语言、音色、消息/段落索引、规范化文本长度与 SHA、音素数量/范围/哈希、语义向量数量/哈希、frontend/acoustic/infer/wav 粗阶段和异常类型；不记录正文、PCM、参考音频或 Key。现有真机矩阵“fresh→zh/en 后首次切 ja 失败，fresh→ja 可用”与中文段落偶发日语听感必须分开归因；+208 以完整 runtime 重建矩阵单独修复，避免与低延迟队列同时改变后无法判断根因。
+7. CPU“超频”同样后排单独 A/B。当前 ORT 固定 8 线程且真机 PSS 曾超过 2 GB，盲目增加线程可能提高发热、降频或触发 isolated service/binder 失败。后续若做，必须提供稳定/快速显式开关，分别记录冷启动、暖机、持续多段 RTF、PSS、温升和失败率；本批不偷偷提高线程或持有三语前端。
+8. 设置导航以底部“更多”为唯一权威中心：先复用现有页面类，把“她/你们”和七域模型、记忆、主动、语音、表情包、设备与数据、诊断全部放入同一入口并逐项核对旧能力；侧栏保留查手机、沉浸房间、她的状态、世界书及素材说明等高频/独有快捷方式，删除主动联系、聊天画面、语音与情绪、文字演出四个重复分类，侧栏“全部设置”只切换到底部“更多”，不再建立第二套设置状态。
+9. 版本目标 `0.41.63+207 / schema 60 / Snapshot protocol 6`。不得回归 +206 的 Gemini 两次有界尝试、DeepSeek 兜底持久提示、异常断流安全草稿、刷新确认、三语投影缓存、紧凑语言按钮、小酒狐四候选、单播放与停止链；动作/神态括号仍按模型偶发格式观察，不叠加 Prompt 禁令或字符串补丁。实现后必须通过新增专项、历史 validators、Kotlin/JVM、Flutter Analyze/全量 tests、arm64 Release、签名与 48 项私有 TTS 实包验证；自动化成功只标 `CI PASSED / APK READY`，不得替代真机低延迟、听感、发热和语言切换证据。
+10. 本地实现已把可见文本预处理升级为 `TtsPreparedUnit(text, role)`，全文模式保留 `dialogue/narration` 语义而不再提前压成单一字符串；队列按单元选择音色，narration 固定 `gentle`、dialogue 使用会话开始时解析的所选/自动音色。所有 gentle 单元向原生播放层传相对 `1.2`，`NativeTtsEngine` 再与用户全局速度相乘并夹到 0.5～2.0；`WavAudioPlayer` 的每段命令携带最终速度，只有速度变化时才等待前序已写 frame 播放完后切 `PlaybackParams`，同速片段继续沿用单一 `AudioTrack`。
+11. 新增 `TtsStructuredStreamParser`，按增量识别沉浸行首 `「/“/"` 为对白、括号或无引号独立叙述为旁白，流中只在句末标点、闭合引号/括号或换行时产出；只有收到完整终止后才由 `finish()` 释放无标点尾部。沉浸 controller 在中文+TTS+自动朗读时于首个单元解析后启动队列并用当时已生成正文做本地情绪判断；成功后 `endStream` 且跳过旧整段重播，Gemini 截断、连接异常、用户 Stop 或重新生成失败路径均先 `stop` 并丢弃未闭合尾部。日/英仍保持完整消息后一次投影/朗读。
+12. TTS 取证已贯通 Dart→MethodChannel→主进程→隔离进程：每个声学分段携带 index；主进程只记录规范化输入字符数与 SHA-256、请求语言/音色、WAV 字节数；隔离 runtime 在 frontend 后记录音素数量、min/max 与序列 SHA，在 infer 后记录 semantic token 数和既有语义 hash，并把粗阶段写入跨进程 checkpoint。成功 `generation_ready` 与失败 `generation_failed/child_process_exit` 都能合并这些字段；`RuntimeDiagnosticStore` 仍是最多 160 条、30 天的脱敏环，不写正文、PCM、参考音频、路径或 Key，因此不会无限增长。
+13. 双 Key 审计未发现 +206 当前运行时的主动消息串线：`ProactiveEngine` 与 internal/Agent/NSFW/摘要/翻译继续读取 `readApiKey/readEndpoint`，它们已硬绑定 DeepSeek；只有普通与沉浸 final expression 读取 `readFinalReply*`。实际补丁针对设置保存窗口：下拉一旦选 DeepSeek就立即启动安全存储写入并显示“后续回复不会再调用 Gemini”，而选 Gemini 仍只在完整保存与独立 Key 校验后启用；显式保存会先等待任何在途 kill-switch 写入，避免快速切换的异步写顺序反转。
+14. 设置导航实现复用一个公开 `SettingsDomainList`：底部“更多”保留“她/你们”，并直接展示模型与联网、记忆与成长、主动联系与感知、语音与聊天呈现、表情包、设备与数据、诊断与开发七域；现有具体页面和 setting key 完全复用。Material 3 侧栏继续保留查手机、沉浸房间、她现在的状态、世界书和素材说明，删除四个重复分类设置卡；“全部设置”关闭侧栏后由 AppShell 切到底部 More，不再 push 第二导航中心。旧 `/settings` route 与页面类暂留兼容已有深链，但不再是主入口。
+15. 本地新增 +207 专项锁定 provider kill-switch/主动 DeepSeek、闭合句流式生命周期、角色音色与 1.2 倍率、PCM 倍率边界、脱敏诊断字段和唯一设置中心；新增 parser、processor 与 queue 单测覆盖中断尾部、旁白/对白和 gentle 倍率。当前容器无 Flutter/Dart SDK，Gradle wrapper也因外网限制无法下载 Gradle，故不能冒充 Flutter/Kotlin 编译已通过。已通过 +207/+206/current-ledger 专项、workflow YAML、`git diff --check`，以及 Actions 同款 86 个 Python 源码门中所有可本地执行项；仅 LingChat 私有恢复、417 桌宠恢复和本机 `kotlinc` 三项因资源/工具缺失不能运行，必须由 Actions 真环境完成。
+
+### 2026-09-11 v0.41.62+206 双通道最终表达、兜底与安全截断草稿（CI PASSED / APK READY / TRUE DEVICE PARTIAL）
 
 1. 用户确认 +205 帅 API 实际无法使用；此前准备评估的 Modelink 尚未进入代码，也不再作为当前首选。用户重新确认 +204 玩游特价模型按请求计费：4 元 100 积分，每个 HTTP 模型请求固定扣 0.5 积分，即 ¥0.02；沉浸一次发送→回复曾观察到只扣 0.5，但用户明确该单次观察可能不完整，不能据此覆盖源码调用链，固定每请求扣 0.5 则为平台明示事实。
 2. 用户提出并认可的方向是“双通道模型”：DeepSeek API Key 必须配置，作为系统内部基础模型；未配置 Gemini 时仍完全沿用旧 DeepSeek 单模型路径。只有用户选择 Gemini 作为回复模型时，才额外填写独立玩游 Key，并让 Gemini 3.7 Flash 负责完成全部 Prompt 注入后的最终可见回复，以利用其文笔与活人感。
@@ -109,6 +127,13 @@
 15. 当前容器没有 Flutter/Dart SDK，因此不冒充 Analyze 或 Flutter tests 已通过。已通过 +206 新专项、当前总账门、+204/+205 保留合同、v0.41.28–50 当前源码专项组、workflow YAML 解析与 `git diff --check`。历史验证器版本允许列表已推进到 +206；完整 Kotlin/Flutter/Release/签名/小酒狐实包门必须等 Actions 实测，在此之前状态严格保持 `CI PENDING / TRUE DEVICE PENDING`。
 16. 首轮 Actions run [`34588108620`](https://github.com/catkiss62/ai-companion-build/actions/runs/34588108620)（836）通过两项超大文件目标 SHA 重建、固定私有载荷恢复、干净基线、全部源码/历史 validator 与 Flutter 依赖解析；在 Kotlin/JVM 步骤触发的 Flutter debug 编译中失败，唯一产品编译错误是沉浸草稿临时 TTS 路径引用 `ChatSegmentCodec` 时漏导入 `models/chat_segment.dart`，两处同源 undefined getter。该轮未进入 Analyze/tests/Release，不得记作功能回归；已补精确 import，下一轮必须从 Kotlin/Flutter 编译重新验证，不能跳过。
 17. import 窄修以远端提交 `613096b0fff553c2f069994091dc667953211608` 触发第二轮 Actions run [`34588761545`](https://github.com/catkiss62/ai-companion-build/actions/runs/34588761545)（837），完整通过超大文件目标 SHA、固定私有资源、全部源码/历史 validator、Kotlin/JVM 与 Flutter debug、Flutter Analyze、`726/726` Flutter tests、arm64 Release、固定签名、48 项小酒狐/Genie/OpenJTalk 实包身份、ORT byte identity、四独立候选、退休语音排除、OpenJTalk ELF、塔罗、checksum、Artifact 与 Draft 上传。APK `AI-Companion-v0.41.62-206-Hybrid-Final-Safe-Drafts-APK.apk` 为 544,680,678 bytes、SHA-256 `c57664d925d39d1fb4d1368a2a8263e4e6e8bcdab0f9432912771d5e41fc3fdf`；Artifact `10195107821` 为 537,801,712 bytes、digest `a80f363a5bbf89c07c98fa18c46759ae66765f0d3f69fac6fc4be44261718c2a`。成功后 Actions 按两项目标 Git blob SHA 回写数据库/总账并删除临时 delta，分支最终 head `3bbc438b0f1f018bb5f791f5b0832b7a39a03452` / tree `4a5d5a605ce96668ca5fda75c47dc6c97afbd1ae`；运行源码与受测字节一致。当前严格状态为 `CI PASSED / APK READY / TRUE DEVICE PENDING`。
+18. 用户提供 +206 当日原生备份与脱敏诊断并明确反馈“重要的真机测试都通过了”。备份 ZIP 完整，Snapshot protocol 6 / schema 60，`state.json` 19,167,204 bytes，实算 SHA-256 `f5db92376707dec4d374e511906f1dd476eb81aff9c5b019fcfb73da45d58320` 与 manifest 一致，媒体清单无缺失。诊断为 `0.41.62+206`；数据库可开、Active Brain、后台 Dart 通道、悬浮触摸/安全位置、系统页面返回后自愈、Accessibility 授权/连接/事件心跳、通知投递与确认、Android 后台条件、音频路由、公网发现和 TTS 核心资源均为 pass；实际运行有 `audio_playback → audio_complete`。`generation_jobs` 为 515 completed / 15 cancelled_by_user / 5 interrupted，当前无 active、failed 或 `awaiting_confirmation`，也无沉浸草稿。因本次未触发 Gemini 故障或异常断流，报告不能单独证明两次重试、持久兜底提示或草稿两选按钮；这些保留为专用故障分支待触发，不影响用户对已实际验收主链的通过结论。诊断为非 deep 导出，故 TTS `integrity=unchecked / artifactCount=0` 只表示未执行黄金哈希深检，不是资源失败。
+19. 用户随后明确更正此前口述，以本条三项有序矩阵为唯一有效真机事实：①首次用中文生成/播放后，切中文或英语均能正确播放，切日语不能播放；②首次用英语生成/播放后，切中文或英语均能正确播放，切日语不能播放；③首次用日语生成/播放后，再切中文、英语或日语均能正确播放。故此前依据最后一条纯中文消息推断“中文前端把中文稳定读成日语”属于证据越界，现已撤销；那条消息和全局 `tts_language=zh` 只能证明中文文本/选择没有被日语缓存替换，不能证明用户实际听到的失败发生在中文前端。
+20. 修正后的矩阵与脱敏诊断互相吻合：报告中三次真实失败均为 `generation_failed / NullPointerException / language=ja`，而不是 `language=zh`；备份同时存在多条已持久化日语 variant，说明至少部分失败发生在外语正文已经生成之后，不是 DeepSeek 没给日语文本。源码当前每次生成都会按目标语言调用 `initialize(language)`，切换时卸载声学模型并 `releaseFrontend()`，随后只构造目标前端；按钮本身不预热，首次播放会惰性准备对应前端。因此“选择中文时禁掉日/英音素”在逻辑上已经成立，不能解决此缺陷。最强候选变为顺序相关的日语原生前端/共享声学 runtime 生命周期：fresh→日语可成功，而中文/英语先装载声学 session 后再释放并切入 `NativeJapaneseFrontend` 时可能留下失效状态或触发内存/原生空引用。+207 应以 fresh→zh→ja、fresh→en→ja、fresh→ja、ja→zh/en→ja 自动与真机矩阵复现，并只在“从非日语进入日语”边界尝试完整重建隔离 runtime；同时把阶段诊断细化为 frontend prepare、acoustic load、infer，不用 Prompt、翻译重试或禁播掩盖原生错误。
+21. 用户明确“三语万能播放”是期望能力：任意历史消息都应能按当前选择读中文、日语或英语；当初考虑 CN/JP/EN 标记只因担心禁用另外两语后无法知道消息首次语言，若万能播放成立则不需要标记。源码证明无需同时预热三语：每个 TTS session 把 `language` 固定到整条队列，`prepareLanguage` 会先卸载旧声学 session、释放旧前端，再只构造目标前端；缺少日/英文本时由 DeepSeek 按需生成并缓存后再播放。因此万能播放与“单一前端常驻”完全兼容，不会让三套前端同时拖慢每段推理。代价仅是切换语言时的冷准备/声学模型重载延迟；可在用户发送新一轮且语言已选定时，与云端 LLM 生成并行准备该单一前端以隐藏延迟，历史消息保持点击后惰性切换。快速连点必须以 generation token 丢弃过期准备，不能出现旧语言晚完成后覆盖新选择。
+22. 沉浸房间当前确实等完整 LLM 回复、续写判定、消息提交以及必要的日/英全文投影全部完成后，才调用 `playText`。底层 `TtsPlaybackQueue` 已具备 `beginStream/addDelta/endStream`，但生产 controller 尚未接入。低延迟优化可先只对中文启用：按 `tts_reading_scope` 在流中缓存到闭合句/闭合 `「」` 后立即入队，未闭合尾部不读；停止或异常断流时立即丢弃尚未播放队列。日语/英语目前依赖完整中文后的单次投影，为避免多次翻译扣费和前后文不一致，继续在全文完成后朗读。此项与日语 runtime 缺陷可同一规划但须独立测试和归因，不应把流式优化当作日语修复。
+23. 本次备份 ZIP 为 35,969,529 bytes（约 34.3 MiB），解压 51,760,824 bytes；其中 30 个媒体文件占 32,588,175 bytes，15 个 `media_blobs` 均有真实引用、无 orphan，消息/相册复用同一 blob，没有发现同图重复拷贝。原图约 24.6 MB，缩略图约 8.0 MB；PNG 缩略图压缩效率偏低，可后续改为更小尺寸/WebP 或 JPEG，但不属于数据重复。`state.json` 解压 19,167,204 bytes，主要增长来自 `desire_events` 22,493 行、`device_events` 15,330 行、消息与感知。进一步源码复核纠正“Desire 无限保留”的初判：`recordDesireEvents` 每次写入都会删除 14 天以前的行，但缺少最大行数 cap，故高频 14 天窗口仍可很大；它只供 24 小时脱敏诊断统计，当前欲望与基线权威状态在 `desire_state`，不参与 Memory/Prompt 回读。`device_events` 已由长期维护按 21 天且最多 12,000 行裁剪，但最近 30 分钟信号、12 小时屏幕状态及上次感知后的新事件会参与设备情境解释并可间接形成短期 Awareness/Desire，因此不能完全停存或随意全删。后续可将两表保留为短期本地运行记录、从跨设备备份中导出为空：Desire 历史改为 24～48 小时且另加行数 cap；Device 历史保留当前判断所需的最近 12 小时并继续 cap。设备与数据页增加带确认的“清理旧运行记录”，明确不删除 `desire_state`、Thought、Memory、关系、聊天或当前 Awareness；若提供全清设备事件，须提示短时间内当前情境会重新积累。
+24. 设置导航审计确认用户所说的重复结构真实存在，但当前源码中的具体位置与口述方向相反：侧栏“全部设置”打开 `/settings` 的七域新页，其中已经有“设备与数据”；底部“更多”仍是旧五域 `她/你们/能力/手机感知/数据与高级`，没有直接的“设备与数据”，只在“能力”中绕回 `/settings`。各具体设置页与 setting key 是共享的，没有两套配置状态，重复的是两套导航中心。后续应以底部“更多”为唯一总入口：先逐项把 `/settings` 的模型与联网、记忆与成长、主动与感知、语音呈现、表情包、设备与数据、诊断与开发映射过去，并保留旧“她/你们”、查手机、沉浸房间、状态、世界书和素材说明的可达路径；覆盖测试证明无独有入口丢失后，侧栏删除四个分类设置快捷项，只留一个“全部设置”直接切到底部“更多”。不能先删除 `/settings` 或页面类，具体页面应继续复用，避免重新实现和保存语义分叉。
 
 ### 2026-09-11 v0.41.61+205 帅 API、沉浸三语与 Gemini 思考摘要（CI PASSED / APK READY / TRUE DEVICE PARTIAL）
 
