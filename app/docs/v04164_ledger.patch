diff --git "a/AI_Companion_\345\275\223\345\211\215\346\200\273\350\264\246.md" "b/AI_Companion_\345\275\223\345\211\215\346\200\273\350\264\246.md"
index 4a109c4..31e3516 100644
--- "a/AI_Companion_\345\275\223\345\211\215\346\200\273\350\264\246.md"
+++ "b/AI_Companion_\345\275\223\345\211\215\346\200\273\350\264\246.md"
@@ -32,28 +32,28 @@
 |---|---|
 | 仓库 | 公开仓库 `catkiss62/ai-companion-build`；完整 Flutter/Android 工程在 `app/` |
 | 持续提交与 APK 授权 | 2026-09-02 用户明确“以后一直允许提交”，并于 2026-09-03 再确认：人机恋项目范围内，可将任务相关源码和文档提交推送到本仓库当前或后续明确的开发分支，并直接执行常规 Actions/APK 创建流程，不再逐批重复询问。此授权不包含合并 `main`、发布正式 Release、删除分支/数据、改变仓库权限或公开密钥/隐私资料；这些仍须单独确认 |
-| 当前开发分支 | `agent/v04163-immersive-tts-settings-hardening`，已构建 `v0.41.63+207 / schema 60 / Snapshot protocol 6`。+207 不直接猜修日语串音/首次切入缺陷，只增强分段与阶段诊断；实现沉浸中文正文闭合句即入 TTS 队列、温柔音色相对 1.2 倍速、全文旁白/动作固定温柔而对白使用已选音色、DeepSeek kill-switch 与两 Key 路由审计，以及侧栏/“更多”唯一设置入口合并 |
+| 当前开发分支 | `agent/v04164-settings-rollback`，目标 `v0.41.64+208 / schema 60 / Snapshot protocol 6`。用户要求先看到还原结果，本批只把侧边栏、底部“更多”、总设置页、设置内说明/交互及对应测试整体还原为 +206 原样；+207 的非设置 TTS/诊断核心保留 |
 | 上一运行代码基线 | `agent/v0417-forthright-fiery-personality`，功能 head `58c244a4b08033f403776f1ec31bbece5557506d`；Desire/Moe/主动性状态主干仍沿革自 `agent/v0415-personality-state-diversity` / `494796ef02e369f98e6896bc5acea7185e3c35dd` |
 | 有效构建 head / tree | +207 Actions build head `6bcb706c00b8c742a2c7494d40f443c67d179e5d`；成功后总账差量回写为分支 head `df6f5db49da66352793846637dde7e75a93e77e4` / tree `427684b669379dfceaf56d11138dec487eb74709`。公开提交不含用户附件、诊断、备份、密钥、RoBERTa、模型权重、参考音频或 APK Artifact；私有小酒狐声学包与三语前端仅在 Actions 中按固定 SHA 分源恢复、裁剪和实包复核 |
-| App / 数据库 | 当前已构建基线为 `0.41.63+207 / schema 60 / Snapshot protocol 6`；继续保持 schema 60 与 Snapshot protocol 6，不做数据迁移。schema 60 仅为沉浸消息增加日语/英语投影正文与分段缓存；中文原文仍是权威内容，Snapshot 继续导出 `immersive_messages` 全列 |
+| App / 数据库 | 当前源码目标为 `0.41.64+208 / schema 60 / Snapshot protocol 6`；继续保持 schema 60 与 Snapshot protocol 6，不做数据迁移。schema 60 仅为沉浸消息增加日语/英语投影正文与分段缓存；中文原文仍是权威内容，Snapshot 继续导出 `immersive_messages` 全列 |
 | 最终 CI | +207 run [`34615837829`](https://github.com/catkiss62/ai-companion-build/actions/runs/34615837829)（843）完整成功：总账目标 SHA、两私有载荷固定 SHA 与分源、干净基线、全部源码/历史 validator、Kotlin/JVM 与 Flutter debug、Flutter Analyze、`731/731` Flutter tests、arm64 Release、固定签名、48 项 Genie/Jiuhu/OpenJTalk 实包大小/哈希、ORT byte identity、四独立小酒狐 case、退休语音排除、OpenJTalk ELF 闭包、塔罗、Artifact、Draft 上传及分支回写均通过 |
 | 测试 APK | `AI-Companion-v0.41.63-207-Immersive-TTS-Settings-APK.apk`，544,697,546 bytes |
 | APK SHA-256 | `6f7c36f93a915512b54ed72f057699d513953acca7b50198a2ce1be9b94b743b`；与 CI checksum 和 Draft 资产服务端 digest 一致 |
 | Artifact / Release | Artifact [`10270877076`](https://github.com/catkiss62/ai-companion-build/actions/runs/34615837829/artifacts/10270877076)，ZIP 537,820,065 bytes / digest `337a7ad02a63530c306b7f0d0f98f8615cad0f2c6bbd8f6f5dd6608ede1dc1c9`；[Draft Release](https://github.com/catkiss62/ai-companion-build/releases/tag/untagged-354d9ff20fda56d7ffa3) 未发布，`main` 未合并 |
 | `main` | 仍停在 v0.38.5 旧基线，未合并 v0.41.x；**不得从 `main` 误判当前项目或作为后续开发基线** |
-| 当前总状态 | +204 小酒狐语音链 `TRUE DEVICE PASSED`；+205 帅 API 不可用，已由后续双通道取代；+206 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`，重要真机主链已通过，但从已播放中文/英语的状态首次切入日语仍有 `language=ja` 的 `NullPointerException`。+207 当前为 `CI PASSED / APK READY / TRUE DEVICE PENDING`：沉浸房间 Genie-TTS 流式朗读、语义音色/倍率、诊断、设置合并与路由硬化均已自动化和实包验证；低延迟、全文旁白/对白听感、实际 Gemini kill-switch 和新增诊断仍须真机确认。动作/神态括号继续只观察，不改 Prompt |
+| 当前总状态 | +204 小酒狐语音链 `TRUE DEVICE PASSED`；+205 帅 API 不可用，已由后续双通道取代；+206 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`，重要真机主链已通过，但从已播放中文/英语的状态首次切入日语仍有 `language=ja` 的 `NullPointerException`。+207 APK 为 `CI PASSED / APK READY / TRUE DEVICE PARTIAL`：TTS/诊断能力待继续真机验收，但设置导航属于已确认产品回归。+208 设置精确回退当前为 `IMPLEMENTED / LOCAL STATIC PASSED / CI PENDING / TRUE DEVICE PENDING`；动作/神态括号继续只观察，不改 Prompt |
 
 ### 3. 当前下一步任务包（新窗口必须完整接住）
 
 | 字段 | 当前内容 |
 |---|---|
-| 当前下一步 | **+207 真机验收**：覆盖安装 APK，先验证底部“更多”包含她/你们与七设置域、侧栏“全部设置”跳到同一中心；切回 DeepSeek 后无需再保存即发一轮普通/沉浸回复，并观察零 Gemini 扣分。沉浸中文自动朗读需确认首个闭合句即开始、完成后不整段重播、Stop/截断不读未闭合尾部；全文模式确认动作/旁白使用默认相对 1.2 倍的温柔音色、`「」` 对白使用所选音色。若再次出现日语首次切入或串音，立即导出新诊断供 +208 单独根修。日语 runtime 与 CPU 快速档继续后排单独 A/B |
-| 目标 | 保持 +206 已完成的双通道：每个正常可见回合通常只产生 1 次玩游固定积分请求，DeepSeek 承担内部机械工作；本批额外保证用户切回 DeepSeek 后无需再按保存即可阻断后续 Gemini 请求，主动消息永远只走 DeepSeek。沉浸中文自动朗读从完整响应后启动前移到闭合句流式入队；全文朗读保留动作/对白结构并按温柔/所选音色分流。设置只保留一个权威导航中心，TTS 失败报告能在不泄露正文的前提下区分语言、段落和 native 阶段 |
-| 当前证据 | +206 已把 `SecureConfig.readApiKey/readEndpoint` 固定为 DeepSeek internal lane，普通/沉浸最终正文独立读取 final lane；当前 `ProactiveEngine` 也只读取 DeepSeek。旧版“关闭 Gemini 后仍强行调用”最可能来自提供商下拉只改页面临时值、必须再点保存才落盘，或来自已被 +206 取代的全局路由。TTS 底层已有流式 A2 队列但生产 controller 未接；全文模式当前把所有段落压成单一字符串和单一音色；原生播放速度当前对整条 `AudioTrack` 生效，若要逐段倍率必须在音色/倍率边界等待已写 PCM 排空再切换。用户最新接受 Desire 14 日、Device 21 日/12,000 行的有界清理，不要求本批改变数据库或备份 |
+| 当前下一步 | **构建 +208 设置精确回退 APK**：将 `agent/v04164-settings-rollback` 推到公开仓库并运行完整 Actions；成品只用于让用户先看到 +206 原样设置基线。不得再次把常用侧栏选项搬入“更多”，不得把完整设置列表复制到“更多”，不得擅自改写“全部设置”的标题、注释或导航。刷新按钮、TTS 日语根修、CPU 快速档和其他功能均不混入本批 |
+| 目标 | 先建立无歧义的旧设置基线：侧栏保留主动联系、聊天画面、语音与情绪、文字演出等高频入口；侧栏“全部设置”继续 push `/settings` 且保留旧注释；底部“更多”恢复她/你们/能力/手机感知/数据与高级五分类；总设置页恢复 +206 的六责任域和原有提示。之后只按用户逐项指令修改 |
+| 当前证据 | `app.dart`、`chat_page.dart`、`chat_quick_settings_pages.dart`、`companion_more_page.dart`、`settings_category_pages.dart`、`settings_page.dart` 及两项信息架构 Widget 测试均已与 +206 commit `36e674597a9c579aec86d555217ab4dd658ec8b0` 做 `git diff --exit-code`，结果为完全一致；+207 沉浸房间 TTS/诊断核心文件未回退。本机缺 Flutter/Dart，故尚无新的 Analyze/Widget test/Release 证据，也没有新 APK |
 | 保护与排除 | 不提交或迁移任何 Key；DeepSeek 与玩游 Key 独立安全保存，DeepSeek 缺失时不得只靠 Gemini 启动完整系统。选择 DeepSeek 回复时不得调用玩游；选择 Gemini 但 Key 缺失/请求失败时须保留用户消息并明确回退或报错，不能静默丢轮。不得把 DeepSeek 内部规划正文或机械 JSON显示给用户；不得让 Gemini 重做工具、伪造 Outcome 或把一次回复拆成无界多次固定扣费。玩游 thinking 请求必须采用真实线端方言，不能原样恢复 +204 把 SDK `extra_body` 当 JSON 字段的错误。动作括号本批只观察，不增加重复禁令、不做字符串删括号补丁；已真机通过的 Genie-TTS 小酒狐四音色、三语、停止与单播放链不得回归 |
 | **媒体 Agent 永久合同（P0）** | **任何“她能发送的媒体”都必须同时具备 Agent 自读能力、可执行工具、真实附件 Outcome、来源 provenance 和发送后第一人称历史；只有 UI、随机表达或 Prompt 声称能力都不算完成。** 表情包先实现明确指令 `sticker.send`；后续联网图、相册图也必须分别接入真实工具。失败、无图库、无匹配、下载失败、权限拒绝或事务失效只能如实返回，不得写成已发送 |
 | 实现边界 | D6 新媒体使用 content-addressed blob；聊天消息、相册与表情发送只增加独立引用/引用计数，不再复制相同原图。保存到相册只增加 album ref；缓存页只列未被永久相册引用的聊天媒体，支持多选、全选和合计空间；删除任一引用都不能破坏其余引用。Snapshot protocol 6 必须携带共享 blob，旧 protocol 5 恢复后仍可手动优化，跨设备或缺少原表情包时历史不破图 |
-| 完成判据 | 新测试锁定：选择 DeepSeek 当刻持久化并使下一次普通/沉浸 final lane 立即变为 DeepSeek，主动/内部/翻译/摘要入口绝不读取 Gemini Key；沉浸中文流在首个闭合句后开始生成且完成后不整段重播，异常断流停止并不朗读未闭合尾部；温柔有效倍率为全局速度×1.2，全文动作/旁白固定温柔、对白使用所选/自动音色，倍率边界不污染相邻音频；脱敏诊断包含语言/分段/哈希/native 阶段而不含正文；底部“更多”可达全部旧、新设置而侧栏不再重复四个分类。完整回归、Analyze、Release、签名和小酒狐实包门全绿后只写 `APK READY`；延迟、发热、日语首次切入、单段日语听感和真实 Gemini kill-switch 仍须真机验证 |
+| 完成判据 | 上述八个设置相关文件与 +206 完全一致，七大规则重新可经“更多→数据与高级→七大规则”进入，+207 非设置核心无意外变化；新增 SHA-256 专项门、历史 validators、Kotlin/JVM、Flutter Analyze/全量 tests、arm64 Release、签名与私有 TTS 实包门全部通过并生成 Draft APK。自动化成功只记 `APK READY`，用户看到实际界面后再逐项指定下一步 |
 | 直接详细入口 | 世界书：`app/docs/WORLDBOOK_2D_PROVENANCE_ROLEPLAY_V04138.md`；造梗来源：`app/lib/core/ai/dialogue_expression_plan.dart`、`app/lib/core/ai/prompt_builder.dart`；媒体合同：`app/docs/SUBJECTIVITY_LIFELIKENESS_AUDIT_2026-09-08.md` 与 `app/docs/STICKER_SEMANTICS_GALLERY_INTEROP_v0.41.51.md`；Snapshot：`app/lib/core/sync/snapshot_service.dart` |
 
 ### 4. 当前任务完成后的后续导航（只导航，不提前展开）
@@ -90,6 +90,13 @@
 
 ## 近期详细记录与全局索引（按需检索）
 
+### 2026-09-11 v0.41.64+208 设置导航整体回退到 +206（IMPLEMENTED / LOCAL STATIC PASSED / CI PENDING / TRUE DEVICE PENDING）
+
+1. 用户明确指出 +207 对设置结构的理解错误：侧边栏里的主动联系、聊天画面、语音与情绪、文字演出都是故意保留的高频入口，不能因为与总设置页共享同一份配置就删除；用户也不会去底部“更多”寻找这些常用设置。底部“更多”不应复制完整设置列表，侧栏“全部设置”的旧标题、旧注释和 `/settings` 跳转也不得擅自改写。
+2. 用户要求为了避免继续误判，先把**所有设置相关内容**完整还原到上一版，再由用户逐项说明后续改法。本次因此不保留 +207 的任何设置 UI/导航/说明/交互改动：`app.dart`、普通聊天侧栏、聊天快捷设置页、底部“更多”、设置分类页、总设置页以及两项对应 Widget 测试均按 +206 commit `36e674597a9c579aec86d555217ab4dd658ec8b0` 原样恢复。+207 的 TTS 流式队列、语义音色、原生诊断等非设置核心不回退。
+3. 精确校验已通过：上述八个设置相关源码/测试文件对 +206 执行 `git diff --exit-code` 返回 0；`validate_v0360_ui_relationship_age.py` 同步恢复旧五分类合同；+207 专项验证器改为锁定恢复后的五分类“更多”、六域总设置、四个侧栏快捷入口与旧“全部设置”注释，同时继续验证 TTS/诊断核心。专项静态门通过。当前容器无 Flutter SDK，无法本地运行 Widget tests/Analyze；全仓历史 validator 直接全量遍历会因 CI 才恢复的私有资源、缺少 Kotlin/Flutter 以及大量退休版固定版本门产生预期失败，不能冒充本次回归失败。
+4. 用户随后要求先构建 APK 看到还原结果；本批因此升为 `v0.41.64+208`，新分支为 `agent/v04164-settings-rollback`。新增 `validate_v04164_settings_rollback.py`，用八个 SHA-256 锁死 +206 设置源码/Widget 测试字节，并额外验证“更多→数据与高级→七大规则”的可达路径。仍不混入“正常完整回复也显示刷新按钮”、日语 TTS 根修、CPU 快速档或新的设置设计；这些必须等用户看到基线后再逐项明确。
+
 ### 2026-09-11 v0.41.63+207 沉浸低延迟朗读、音色语义、设置归一与路由硬化（CI PASSED / APK READY / TRUE DEVICE PENDING）
 
 1. 用户接受当前运行记录的有界清理事实：`desire_events` 写入时删除 14 天以前记录，`device_events` 由维护任务按 21 天且最多 12,000 行裁剪；短时间高频导致备份暂时增大可以接受，只要求冗余不会永久无限增长。故 +207 不改变 schema、不清空历史、不新增删除按钮，也不把参与近期设备情境判断的事件与纯诊断事件误用同一删除策略；后续只有出现无界增长或备份负担的真机证据才重开。
