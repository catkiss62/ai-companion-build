# v0.41.87+231 帮助页与游戏厅活动窗真机验收增量

> 本节只验收 +231；帮助页是能力说明，不会改变 Agent 权限、路由、记忆或模型调用。

1. 打开“全部设置”。预期：顶部出现“帮助与真实能力”，可进入；页面能看到系统检查、普通聊天可调用、她可自主使用、停止/刷新、网页图片与查手机、Cedar、本地语音、备份、权限隐私、尚未开放和故障排查。
2. 展开“普通聊天可调用”“她可自主使用”“尚未开放”。预期：Cedar 专用能力显示为可用；视频理解、真实提醒、记忆/人设/规则修改提案和通用 MCP 明确显示尚未开放，不冒充已经实现。
3. 在普通聊天输入 `【检查系统】看看你有哪些真实功能`。预期：仍调用真实 `system_self.read`；帮助页本身不触发模型、不写记忆，也不改变原有自然语言 Agent 路由。
4. 打开游戏厅活动窗并滚动到“最近进展”。预期：正文位于居中的窄卡片，左右存在可触发外层滚动的空白；长按正文仍可选择文字，活动窗其他区域不变。
5. 在“活动记录”点击一条被截断的记录。预期：弹出“活动记录详情”，能滚动查看完整内容、动作和时间；有图片或查看页时仍可查看，关闭后回到原活动窗且游戏状态不改变。
6. 回归检查：活动窗拖动、标题栏最小化/关闭、右下角缩放、切换旧游戏存档、游玩节奏、暂停/恢复和远程图片均保持原行为。

# v0.41.86+230 单人活动重仲裁与 TTS 诊断真机验收增量

> 本节只验收 +230；+228/+229 未完成的真机项继续保留，不因版本递增视为通过。

## A. 高兴音色与参考音频取证

1. 自动音色下生成一条明确 `happy / 高兴` 回复，预期使用 `cute / 可爱 / jiuhu_devotion`；`excited / 兴奋` 与 `surprised / 惊讶` 仍使用 `lively / 活泼 / jiuhu_idle50`。固定音色不受影响。
2. 若再次听到疑似参考录音，立即导出同一时刻诊断。TTS `generation_ready` 与 `lastProcessCheckpoint` 应包含 language、voice、referenceCaseId、输入/规范化字符类别计数、validPhoneCount、decoderIterations、immediateStop、semanticCount/hash、pcmDurationMs/hash 和 `referenceEchoSuspected`，但不含正文或参考文件路径。
3. 四个自动/固定音色各播一次，确认 referenceCaseId 分别可落到 `jiuhu_bento_tools / jiuhu_dream_days / jiuhu_idle50 / jiuhu_devotion`。本版只取证，不因 `referenceEchoSuspected=true` 自动静音或篡改输出。

## B. 单人 episode checkpoint 与统一竞争

1. 让她独自运行同一局会改变远端状态的游戏。前三个成功 mutation 之间仍按原休闲/观看节奏连续；第三个之后下一次续步必须先形成 `episode_checkpoint`，不能直接第四步。
2. checkpoint 时应把 `resume_game` 与联网发现/分享、普通主动联系、休息和沉默放入同一现有选择器。其他行为胜出时远端存档不丢、游戏不换局；之后可恢复同一局。没有第三个 mutation 时，持续 25 分钟也应形成 checkpoint。
3. 只读 state/status/rooms、失败 Outcome、平台 announcements/rest、leave/resign 不计入三步。实时共玩轮到 companion 时仍立即履约，不等待单人 checkpoint。

## C. Cedar 防沉迷

1. 普通游戏内“门已锁/区域冷却”不得触发防沉迷。只有服务端结构化 anti-addiction 字段或明确包含“防沉迷”的提醒/锁定文本才形成强制 checkpoint。
2. 提醒后普通恢复可参与竞争但带成本；锁定且恢复时间未到时不得出现普通 `resume_game`。诊断只记录 level/source/permission/recovery time，不记录 Outcome 正文。
3. 网站关闭 `allow_self_reset` 时，APK 绝不能调用 `rest`。网站打开且服务端真实返回允许时，`self_reset_and_resume` 仍须赢得统一竞争才调用一次；成功后下一步按休闲 2 分钟或当前观看节奏，绝不能 1 秒续跑。

---

# v0.41.85+229 Cedar 结构化远程媒体桥真机验收增量

> 本节只验收 +229。+228 的 Agent 循环、Token、联网、自主性、手机时间线、刷新和音效项目继续保留在下方，不因版本递增视为通过。

## A. 旅行图片进入真实聊天附件

1. 在普通聊天中让她继续一次会返回 `photo_url` 的旅行景点/餐饮步骤。
2. 预期：工具真实成功后，同一条 assistant 消息出现 1～3 张可打开的图片附件；附件来源为 Cedar，不是公共网页搜索或本地相册伪装。
3. 打开游戏厅活动窗。预期：最近进展显示的图片与聊天第一张附件来自同一份本地固化字节；活动记录图标为图片。
4. 关闭网络或令图片地址失败后再推进一步。预期：游戏文字结果仍正常保留，不出现空白回复，也不得声称“图片已经发出”。

## B. 下矿/其他 Cedar 明确媒体字段

1. 运行一次会返回 `image_url`、`imageUrl` 或 `images[].url` 的下矿/其他游戏步骤。
2. 预期：只有协议字段内的 HTTPS 图片被下载并附加；普通叙述中偶然出现的网址不产生附件。
3. 同一 Outcome 有重复 URL 或超过三张远程图时，预期去重且远程附件最多三张；若同时存在 MCP 标准 Base64 image，总附件最多四张。

## C. Stop 与失败降级

1. 在大图下载期间点击 Stop。
2. 预期：当前可见生成尽快停止，不需要杀后台；未交付附件被清理，取消后不得迟到插入聊天消息。
3. 重新发送普通消息。预期：下一轮可以立即开始；已经真实发生的 Cedar 文字 Outcome/游戏状态仍保留。

## D. 取证

- 导出同一时刻脱敏诊断与备份，核对 Cedar event 的 `content_kinds` 含 `image`、`image_reference` 非空，最终 assistant message 含 `assistant_mcp_image:cedar:` 附件来源。
- 只有旅行与下矿至少各一次成功、一次失败降级且 Stop 通过后，+229 才可记为 `TRUE DEVICE PASSED`。

---

# v0.41.84+228 Agent 循环与自主性闭环真机验收增量

- Token/调用：保持一局单人游戏活跃后连续进行三次普通非游戏聊天；每条聊天最多一次前台 Cedar mutation，且普通聊天不应仅因 session active 注入整套游戏指南。后台每个 cadence 最多一次规划/一次推进；诊断 `modelUsage.byLane` 能分别看到 final reply、Agent planning、Cedar background/outcome 与 web lanes，不含正文或密钥。
- 实时共玩：网页端落子并返回权威 `next_actor=companion` 后，小机同一 execution 及时回恰好一步；不得受单人疲劳 Gate 拖延，也不得在同一 tick 连续替双方下棋。
- 参数与恢复：让模型第一次给出合法 action 但缺业务对象，确认网络写入前被阻止并只重规划一次；唯一 `rooms` 结果自动 `state(full_state=true,wait=false)`，多个房间绝不本地猜选。导入旧 `duel · 轮到你 · last_action=rooms` 状态时先显示/记录同步，权威 state 返回后才恢复 actor。
- 终局：Cedar 返回 winner/result/finished 后，即使 session 已 completed，紧邻普通聊天仍知道胜负；可见消息成功持久化后 pending terminal 清除。杀进程后恢复可补发一次，但普通回复和主动消息不得重复交付同一个 terminal key。
- 自主节奏：单人游戏可继续，但深夜、疲劳高或近 6 小时动作饱和时会让休息/联网/其他念头重新赢得机会；已承诺真人共玩不参加该竞争。不得靠降低钓鱼固定概率验收，因为当前没有固定逐游戏概率表。
- 联网：`看看有什么好东西`、钓鱼语境的`看看氧气瓶`不触发公网；`上网看看氧气瓶资料`、明确 URL、今天的天气仍可搜索。网页搜索需读到来源 key points/uncertainties/read_at；点击 Stop 后很快取消，无需杀后台，迟到结果不写消息或候选。
- 自主发现/相册：自主 public-web 不再出现 registry denied；候选经真实 search/read/appraisal 入库并可自然分享。相册 public-web 优先，FishArchive 备用只用 PNG/JPEG original，一个坏图不烧掉全天，最多三次有界尝试。
- 主动联系：最后一条真实普通消息后不足 10 分钟不出站、不消耗候选/Thought/次数；第 8 分钟再次聊天后继续顺延到新的 10 分钟。窗口结束后新话题仍是新话题，不改写成棋局 followup。
- 手机时间线：熄屏一小时后刚亮屏操作两分钟，只能判断刚拿起/本次亮屏两分钟；不能说连续使用一晚上。持续亮屏 40 分钟仍允许判断用了一阵，熄屏前活动可作为短时历史但必须明确“之前”。
- 表达/UI：慢 Gemini 下情绪音效与可见正文提交同步，隐藏 `<emotion>` 先到时不提前播放；普通活泼/游戏操作不默认选“调皮”。确认刷新回复后保持锚定聊天底部。规则 05 新句与最新用户版“性格光谱”存在，后续用户编辑不被迁移覆盖。

# v0.41.83+227 Cedar 原生 Agent 续接闭环真机验收增量

1. 覆盖安装且不清数据，复用诊断中已有的双弈房间。诊断为 `active/multiplayer/nextActor=companion/lastAction=state` 时，不在 APK 再说“动一下”；后台必须使用与前台相同的原生工具调用解析链直接选择并提交合法 `move`。
2. 在 Cedar 网页连续完成至少五次“用户落子→伴侣落子”。`state(wait=true)` 一旦返回 `your_turn=true`，同一个后台 execution 必须立即规划并落子，不能先结束为 `remote_event_companion_turn` 再等下一次 heartbeat。
3. 已建立的远端 session 不再逐步经过疲劳、夜间或 Desire 二次裁决；只有用户聊天写租约、停止、备份/恢复 freeze、Cedar 双开关和 execution fence 可以抢占。新游戏的自主选择仍按 Desire 节律，服务端防沉迷仍按真实 Outcome 处理。
4. 抓取一次后台模型请求，必须带唯一 `cedar_toy_play` schema 且 `tool_choice=auto`，不得使用后台专有的 `required` 请求形状。SSE 分片工具名/参数由前后台共享的 `AgentNativeToolCallAccumulator` 组装。
5. 让 Provider 返回 HTTP 400/401/429/5xx。诊断必须显示 `provider_http_<status>` 与脱敏、限长的 `lastExecutionErrorDetail`，不得再只显示 `other`，且不得包含 Token、房间号、聊天正文或完整参数。
6. 回归错误游戏、重复只读动作、损坏参数和零工具调用：最多一次纠正后有界失败并释放动作租约；不得重放不确定写动作。自主 origin 只允许 registry 中 `autonomousAvailable=true` 的能力，表情包、屏幕观察等不能借游戏后台越权。
7. 回归普通聊天、停止、表情包、保存/恢复备份、TTS、相册、Memory、schema 61 和 Snapshot protocol 6。只有 Actions 的源码门、Kotlin/JVM、Flutter Analyze、全量 Flutter tests、arm64 Release、签名和 APK 校验全部通过后才提供这一个候选包；CI 通过仍不冒充真机通过。

# v0.41.82+226 Cedar 权威状态机端到端闭环真机验收增量

1. 覆盖安装且不清数据，说一次“我们下五子棋，你建房，我加入”。同一轮应自行完成目录/指南/建房；`new` 的系统回包不应再等满 25 秒后显示网络超时。若 AI 先手，应继续自动落第一手；若用户先手，应立即进入远端等待，不要求再说一步。
2. 在 Cedar 网页加入并连续下至少五个双方回合，之后不要在 APK 主聊天催促。每次网页落子使 `your_turn=true` 后，她都应由后台 Agent 自动读取权威 revision、选择合法 `move`、落子并重新挂等；活动窗关闭或普通聊天页保持可见不能中断这条链。
3. 在网页房间随一次用户落子发送消息。当前上游协议不会用“只有普通消息、尚未轮到小机”单独唤醒；真正轮到她时，消息必须随 events 到达，并由她在同一 `move` 或允许 message 的状态动作中回复。不得误报网页没有消息，也不得重复回复同一事件。
4. 模拟一次 `new` 回包丢失和一次 `move` 回包丢失。APK 不得重放写动作：前者先 `rooms` 找回唯一活跃房间，后者只用 `state(full_state=true,wait=false)` 对账；随后继续真实房间，而不是再建一间或重复落同一手。
5. 保持普通聊天页可见但不发消息，确认 Cedar 仍续跑；在她规划或长轮询时真正发送聊天，`chat_turn_lease` 应在约一秒内抢占并正常回复，Cedar 保存进度后再续。点停止后 `awaiting_confirmation` 也必须终止；随后立即发言、发送表情包、保存普通备份和恢复备份均可用。
6. 在 00:00–07:00 且没有打开游戏厅观看时，已有 Cedar session 应显示夜间休息并延后到 07:00，强 Thought 不能让钓鱼/其他单人游戏整夜循环；用户主动打开活动窗观看时可以明确覆盖。关闭“Cedar Toy”或“允许她自主玩”任一开关，当前执行应被 fence、活动暂停、next-action 时钟清除。
7. 导出脱敏诊断应显示 `enabled/autonomyEnabled/lastContinuationState/lastExecutionErrorCategory`，不得包含 Token、房间号、参数或聊天正文。正常成功后 execution error 应清空；第一次 planner timeout 可累计一次 `agentActionRetryCount`，不能再静默退避五分钟。
8. 回归普通聊天、用户/伴侣表情包、保存/恢复备份、TTS、相册、Memory、schema 61 和 Snapshot protocol 6。本版不把 CI 绿灯等同真机通过；只有上述连续回合和基础功能均通过才记 `TRUE DEVICE PASSED`。

# v0.41.81+225 Cedar 后台原生 Agent 工具闭环真机验收增量

1. 覆盖安装且不清数据，复用现有双弈房间或重新建房。用户只在 Cedar 网页发消息并落子，此后不要在 APK 主聊天说“下好了/该你了”；活动窗应在读取 `your_turn=true` 后自主执行一次合法 `move` 并把短对白随同一动作发进房间。
2. 导出诊断：后台动作规划正常时 `agentActionRetryCount` 不增加；若第一次没有函数调用、返回错误游戏或重复 `state`，只允许增加一次并在第二次改成真实推进动作。旧 `jsonRetryCount` 不应因后台棋步继续增长。
3. 连续完成至少三次网页用户→伴侣换手。每轮必须由原生 `cedar_toy_play` 工具调用承载 action/params；不得把自由 JSON 正文当动作，不得要求用户回 APK 逐步催促，也不得每 15 秒重复查询同一状态。
4. 让 Provider 连续两次不给工具或连续选择只读查询。单轮应有界结束、清除“规划下一步”动画并释放 Cedar 狭义动作锁；随后普通聊天、停止、用户表情包、保存备份和恢复备份都必须可用，不能被游戏厅拖死。
5. 回归游戏厅双开关和夜间节律：关闭任一开关会抢占规划并暂停 session；夜间疲劳/休息胜出时按既有分钟级 Gate 延后。重新开启后可以从保存的远端状态继续，但不得绕过 Gate 永久钓鱼。
6. 回归前台同轮建房：说“我们下棋，你建房，我加入”仍应按 `list_games → get_guide → rooms/catalog → new` 自主推进；服务端 `next_call`、写超时同步、Stop 后 Outcome 持久化和无路由等待释放保持 +224 合同。

# v0.41.80+224 Cedar Agent 完整续接链真机验收增量

1. 覆盖安装且不清数据，普通聊天直接说“我们下棋，你建房，我加入”。同一个用户回合应自主完成 `list_games → get_guide → 必要的 catalog/rooms → duel.new`，只有真实 Outcome 返回后才宣布建房；不得要求用户把“建房”再说一次。
2. 她建房后在 Cedar 网页加入、发一条房间消息并落子，此后不在 APK 主聊天催促。服务端 `next_call` 必须独立于本地 participation_mode/旧邀请标记保持挂等；轮到她后自动读取新状态、生成房间回复并执行合法棋步，然后重新挂等用户。
3. 让目录、指南或只读房间查询后的规划模型第一次返回零工具调用。每个真实阶段最多复核一次，仍在 10 规划轮/16 调用的硬预算内继续；不得把“查到目录/指南”当成整项下棋任务完成，也不得无限循环。
4. 在一次 Cedar 写请求远端已经返回后立刻点停止键。停止键应结束可见回复，但真实 Outcome、next_actor、next_call、房间消息去重键和远端别名必须先持久化；重新打开或等后台唤醒后从该状态继续，绝不原样重放写动作。
5. 制造写请求网络超时。前台只记录“结果未知”并进入同步，不得让整条聊天异常退出，也不得重放 `new/join/move`；若是服务端已签发的精确 next_call，即使精简指南未重复动作名也允许按原参数观察。
6. 对已有活动房间再次读取指南。只更新指南元数据，不得把 active/waiting/paused 重置成 guideReady，不得清空 next_call、pending room message、房间别名、next_actor 或用户许可。
7. 回归关闭“启用 Cedar Toy”或“允许她自主逛游戏厅”：正在规划立即被抢占、会话本机暂停、夜间普通单人活动进入休息延迟；普通聊天、停止、表情包、保存/恢复备份仍可工作，不得再被钓鱼规划长期占住。
8. 在“花园与猫”或任一游戏制造服务端仅返回 `next_actor=wait`、但没有 `next_call` 且没有 `resume_after_seconds` 的结果：本局远端进度应转为可恢复的暂存状态并立即释放当前游戏位，不能永久显示“等待游戏”，也不能阻止后续自主周期选择其他游戏。有 `next_call` 或明确唤醒时间的等待仍必须保留并按时续接。

# v0.41.79+223 Cedar 运行时抢占、开关与夜间节律真机验收增量

1. 复现旧问题：让钓鱼或任意 Cedar 步骤停在“正在执行 / 规划下一步”，立即发送一条普通聊天。前台聊天应在约 1 秒内抢占 Cedar，转圈消失且正常回复；不得等待 30/120/240 秒，Cedar 的迟到回包也不得写回 session。
2. Cedar 正在规划或请求 MCP 时，分别关闭“Cedar Toy”总开关和“允许她自主玩”开关。两者都必须立即清除执行状态并暂停当前游戏，保留远端存档。只有两个开关都恢复时才恢复因开关暂停的对局；不得自动恢复用户手动点过暂停的对局。
3. 在本机时间 04:40 左右且用户未观看时，已开始的单人游戏也应进入 `night_sleep`，直接休眠到早上 07:00；夜间再强的本局 Thought 也不能让无人观看的游戏继续机械运行。白天仍按 Desire/Thought/困意竞争，但不得因为 session 已存在就永久绕过休息。
4. Cedar 处于阻塞请求时点“保存备份”。冻结必须抢占 Cedar，`cedar_toy_action_lease_until` 在短时内释放，备份继续生成并打开系统保存页；不得再被 `recovery_orchestrator_lease_until` 占满 90 秒。恢复备份同样不得半覆盖或导入旧的转圈执行态。
5. 让 DeepSeek Cedar JSON 请求不返回：单次最多等 30 秒，超时后本轮不得立即再等第二个 30 秒。空 JSON/JSON 损坏/429/5xx 仍可最多改变策略重试一次。
6. 联合回归：停止一条正在回复的主聊天后立即新发言、发送用户表情包、触发伴侣表情图、保存与恢复备份，均应正常。共玩房间中用户在网页发言并落子后，她仍应无需 APK 内催促就自动回话和执行合法下一步。

# v0.41.78+222 停止与备份互锁真机验收增量

1. 发送一条会长时间停在“正在回复”的消息，立即点停止。按钮应在旧前台或后台 Provider 连接真实关闭、`chat_turn_lease` 释放后恢复；不得只清空动画却继续占用写入通道。
2. 停止旧回复后立即发送新消息。新消息必须正常进入回复链；旧请求的迟到数据不得落成助手消息、Memory、Cedar 动作或后台整理结果。
3. 在旧回复仍卡住时打开备份页并保存普通备份。冻结应主动终止后台模型等待，再等所有写入租约释放；生成并校验 ZIP 后先解冻，再打开系统保存页。文件选择器停留多久都不得冻结聊天。
4. 连续快速执行“保存→取消系统保存页→再次保存”和“选择恢复→取消确认→再次恢复”。旧页面的 `finally` 不得清除新操作的 `transfer_lock_owner`，也不得出现“创建普通备份前必须先冻结本机写入”。
5. 选择一份已验证的普通备份恢复。若仍有写入者，错误必须显示具体“阻塞项”；超时不得半覆盖原数据。成功恢复后聊天、保存和再次恢复均可继续。
6. 导出脱敏诊断，确认包含各状态写入租约的 held/到期信息但不包含 owner token、聊天正文、房间消息、凭据或备份内容。

# v0.41.76+220 Cedar 玩家协议与后台连续行动真机验收增量

1. 覆盖安装后直接说“我们来玩五子棋，你建房，让你先手”。她应自行完成真实目录/指南/必要 catalog 读取，并提交 `duel.new`；只有真实 Outcome 返回房间号后才能说已经建房。活动诊断应显示 `playerProtocolCached=true`。不得要求用户再说一次“开房”，不得把计划说成完成。
2. 在 Cedar 网页加入她建立的房间并落子，此后不要在 App 主聊天发送“我下好了”。她应通过持久 `state(wait=true)` 收到新 revision，并在轮到自己后自动规划、落子，再恢复挂等。一次空 JSON/不完整 JSON 可触发一次窄重试；诊断的 `jsonRetryCount` 可增加，但房间不能因此永久停在队列。
3. 在网页房间同时发送一句普通中文。它可暂存为 `pending_room_message`，但应随她下一次合法 `move/state` 由 DeepSeek 生成房间内回复并清空；诊断仍不得包含房间正文、room id 或 continuation 参数。
4. 让第一次发现轮只查到 `rooms/catalog`。若目标尚未完成且仍有总预算，应出现最多一次内部无调用复核并继续真实动作；`noCallRecheckCount` 可增加。第二次仍无调用时必须诚实说明缺口，不允许无限循环。
5. 盲玩回归：要求她看 GitHub、源码、人类攻略或文字推理答案时，仍不得获得网页工具；实时 `play` schema 与本机最小协议补充只包含参数签名，不包含棋谱、落点策略、题目答案或隐藏状态。

# v0.41.73+217 双弈确定性入口与回复完整性真机验收增量

1. 从 `v0.41.72+216` 覆盖安装，不清数据；确认版本 `v0.41.73+217`、schema 61、Snapshot protocol 6，旧聊天、Memory、规则、相册、TTS、Cedar 多游戏 session 和远端存档保留。
2. 保持旧钓鱼 session，在普通聊天明确说“去游戏厅里玩双弈吧”。本轮必须真实读取 catalog 中 duel 的完整指南，并在同轮依指南选择查房/进房/需要的下一步；不得只说自然语言而零 Cedar 调用，也不得删除钓鱼进度。
3. 在已有共玩/等待邀请的 Cedar 上下文中，单独发送房间码 `5JH5MDVT`。它应被视为现有房间信息与共玩许可，不得反向要求用户接受她的邀请；无 Cedar 上下文时普通字母数字串不应自行打开游戏工具。
4. DeepSeek 单模型与 DeepSeek+Gemini 各测一次最终回复截断：`length/content_filter/safety/error`、有半段正文但无流终止帧，或以 `「` 开头却未闭合。半句不得落入正式聊天/上下文/记忆，UI 必须明确提示“重新生成”或“保留这段回复”；两个操作分别能成功解除阻塞。
5. 观察一次游戏分享或普通主动消息的截断注入：第一次不完整时只内部重试一次，连续不完整则不发送、不落库、不通知。不得再出现像 `「我现在满脑子` 这种已提交半句。
6. 当聊天页最新一条是主动消息时，不再显示无法执行的刷新按钮；最新普通用户回合的已完成回复仍可正常重新生成，待确认截断草稿的刷新/保留按钮仍可用。
7. 小酒狐手动音色列表只剩“自动判断 / 日常 / 温柔 / 活泼 / 可爱”，“轻语 / 耳语 / 气声”不再出现；从旧版固定在三者之一覆盖安装时应安全回退为自动。APK 生产 manifest 只含四个已验证音色，不再恢复七候选 overlay。
8. 回归检查单人休闲 2 分钟、快速 5 秒、观战 10 秒、共玩实时续接、服务端 `next_call/next_actor/terminal/revision`、小酒狐四音色 TTS；不得因本窄修改回归。

## v0.41.72+216 Cedar 信任优先与临时观战节奏真机验收增量（历史回归项）

1. 从 `v0.41.71+215` 覆盖安装，不清数据；确认版本 `v0.41.72+216`、schema 61、Snapshot protocol 6，旧聊天、Memory、规则、相册、TTS、Cedar 多游戏 session 和远端存档保留。
2. 打开游戏厅活动窗，默认应选中“休闲模式”；切到“快速模式”后单人游戏按“每次真实操作完成后至少 5 秒”继续，切到“观战模式”后至少 10 秒。关闭再打开活动窗，必须恢复休闲模式（至少 2 分钟），快速/观战不得成为永久后台设置。
3. 用钓鱼等单人游戏验证：没有 Cedar 明确的终局/等待状态时持续由她推进，不再显示“轮到你”，也不需要在主聊天反复说“我下好了”。
4. 用双弈等共玩游戏验证：玩家在 Cedar 网页落子后，她通过服务返回的 `next_call` 自动接收并尽快回应；落子写请求不得因 `wait=true` 长轮询而出现“远端已下、本机仍认为失败”。无事件的长轮询超时只续订，不显示失败或触发五分钟退避。
5. 快速/观战模式里制造一条值得分享的真实进展，确认它仍先落为 Cedar Thought，但随后直接走正常主聊天生成人设链发送；休闲模式的同类进展只进入 Thought，等待正常 Desire/Gate 自然分享。
6. Gemini 为最终回复提供方时，在 Cedar 房间产生一条短对话：应使用低隐藏思考、512 token 上限成功发到网页房间；若真实供应商仍报错，可显示 DeepSeek 兜底通知，但正文不得出现 JSON/XML/DSML/工具名、参数或代码围栏。
7. 检查结构化 `next_actor`、终局、`next_call`、revision 与合法动作：本机以 Cedar 明确事实为准，不再用第二次模型判决覆盖；本地只保留 Active Brain、用户对话租约、沉浸页、单写操作、凭据/隐私和正文真实性边界。

## v0.41.71+215 Cedar 实时共玩与房间对话真机验收增量（历史回归项）

1. 从 `v0.41.70+214` 覆盖安装，不清数据；确认版本 `v0.41.71+215`、schema 61、Snapshot protocol 6，旧聊天、Memory、规则、相册、TTS、Cedar 多游戏 session 和远端存档保留。
2. 复用 +214 留下的双弈房间或新建一局；用户在 Cedar 网页落子后不再发“我下好了”。活动窗应显示“已挂等房间事件”，她应在网络/模型正常延迟内自动接招，不依赖主聊天新消息唤醒。
3. 她落子后连续完成至少 5 个来回；每次都必须消费远端返回的 `revision/current_actor/next_call`，不重放旧 move，不在用户回合抢走。断网 30 秒再恢复，应重新同步而非连续落重复子。
4. 专门验证 +214 的嵌套结构失败夹具：`room.current_actor.participant_kind=bound_machine`或等价字段应被识别为她；不得再落成 `next_actor=wait / waiting_remote`。
5. 在双弈网页房间聊天输入框发送两条消息（一条在她回合，一条在用户仍未落子时）。她应读取公开 `events[].message`，只回复新的对方消息，并通过 MCP `message` 出现在同一网页房间；不得回复自己、重复刷屏或要求用户切回 App。
6. 单独测海龟汤或另一个指南声明支持 `message` 的 Cedar 共玩项目：房间消息桥不依赖 duel/五子棋 ID；若该 action 指南没有授权 `message`，App 不得擅自添加参数。
7. DeepSeek+Gemini 模式中，每个真实房间回复仍由 DeepSeek 做内部动作/意图规划，再由一次 Gemini 形成可见房间台词；Gemini 失败时可用 DeepSeek 兜底，但网页和 App 正文均不得出现 JSON、DSML、XML、tool_calls、参数片段或代码围栏。
8. 复现真机残片 `"revision": 3, "wait": true, "move": {"row": 5,`；它必须被识别为不完整机器 payload，不落库、不进 segments、不被 TTS 朗读。任何工具结果后的零 tool-call 规划响应都必须另外进入最终表达阶段。
9. 当本机真实提交 `move.row=6,col=6` 时，诱导最终表达改说 `(7,8)`；失配句必须被事实守门拦下。最终可以不报坐标，但一旦报出必须与已跨过本地执行门的真实参数一致。
10. 在活动窗点“暂停自主游戏”后，房间挂等与回话均停止；恢复后继续。终局、退房、认输、权限/凭据缺失时不再请求。“打开 Cedar 官方游戏厅”必须只打开 `https://toy.cedarstar.org/`。
11. 导出脱敏诊断与备份：Token/密码/绑定码不出现；诊断只显示挂等状态、去重计数、最近错误类别与 Provider 兜底状态，不包含房间消息正文。TTS、人格、相册、媒体、schema 61 与 Snapshot protocol 6 做回归冒烟，不得借本包改动。

# v0.41.70+214 Agent/MCP 通用运行时加固真机验收增量

1. 从 `v0.41.69+213` 覆盖安装，确认版本 `v0.41.70+214`、schema 61、Snapshot protocol 6；旧聊天、Memory、规则、相册、TTS、Cedar 多游戏 session 和远端存档均保留。
2. 用户先创建共玩房间并明确邀请她加入时，她应把该邀请直接视为双方参与许可，读取真实列表/指南/房间状态后加入或行动；不得颠倒成“她邀请用户”并再次等待同意。用户明确拒绝时仍不得执行共玩写动作。
3. 在一个回合制游戏中让用户先完成一步，使真实结构化状态表示 `human=X`、`bound_machine=O` 且 `current_actor` 属于 AI。她必须按结构化身份认出自己是 O 且现在轮到自己，随后在同一有界工具循环继续执行指南允许的下一步；不得仅凭棋子颜色猜先后，也不得错误停在 `waiting_remote`。
4. 让 Provider 在第一轮或任意后续规划轮以 `<｜｜DSML｜｜ calls>` 文本返回合法工具调用。该文本必须被规范化后继续经过既有 Registry、用户意图、schema、预算与执行 Gate；正文不得出现 DSML、`function_call`、`tool_calls`、参数或内部调用代码，流式 TTS 也不得朗读这些内容。
5. 返回残缺、混有对白或无法解析的机器协议时，系统不得执行；最终输出防线应阻止它落库。若其中另有独立自然语言句子，只可保留通过真实性校验的正文；纯协议草稿不能由“保留这段回复”写入聊天。
6. 活跃共玩尚未结束时，预先积压的其他 Cedar 游戏分享不得插入当前对话；它应保持未消费并延后。共玩结束后再按原主动联系频率 Gate 处理，不能因此丢失或重复发送 Thought。
7. DeepSeek 单模型模式正常完成多轮工具规划/核验/最终回复；DeepSeek+Gemini 模式仍由 DeepSeek完成内部规划和核验，外部 MCP 仅返回真实数据，所有结果收齐后只调用一次 Gemini 生成最终可见回复。
8. 用另一个返回常见 `next_actor/current_actor/participants/seat/status` 结构的测试 MCP 场景做冒烟，确认通用解析器不依赖 Cedar 游戏 ID、五子棋、黑白棋子或固定动作名；结构不明确时允许 DeepSeek 判断，但不得把不确定结果冒充协议事实。
9. 导出脱敏诊断与备份，确认账号、密码、Token、绑定码、房间私密内容和内部工具参数未进入可见消息；本版不改数据库 schema、Snapshot protocol、TTS 引擎和人格/成人表达规则。

# v0.41.69+213 Cedar 切换与后台续步真机验收增量

1. 从 +212 覆盖安装并恢复 2026-09-13 真机备份；确认版本 `0.41.69+213`、schema 61、Snapshot protocol 6，原钓鱼进度和 Outcome 仍在，活动窗另出现由原文提取的 `fishing → duel / rooms` 跨游戏提醒，原文不得被改写或删除。
2. 钓鱼没有在途网络动作时说“快来双弈，我已开五子棋房间”，应先取得 duel 的完整真实指南，再按指南进入；工具不得再返回 `action_not_in_current_guide`，也不得用 fishing 指南授权 duel。
3. 在钓鱼动作明确显示“正在执行”时提出相同切换，回复应诚实说明当前动作完成后切换；活动窗显示可靠队列。不得强行取消在途动作，也不得声称“这就杀进去/已经进房”。动作结束后后台自动读取 duel 指南并切换，钓鱼 session 仍可在活动窗查看。
4. 用户已主动邀请并给出房间码时，系统可把该轮作为明确共玩同意，不应机械地再要求 AI 反向邀请一次；若只有想玩、没有用户同意，纯共玩/多人游戏仍必须先邀请。混合模式可独自开始，但进入其中的共玩分支仍需明确同意。
5. 开始一个 `next_actor=companion` 的单人多步游戏后停止发消息；活动窗应在约两分钟节拍或指南/Outcome 明确等待时间到期后继续出现真实步骤。每拍只做一个动作；轮到 user/shared、无明确时长的 wait、paused、finished 或 failed 时停止后台续步。
6. MCP play 返回后，状态中的 `next_actor/share_level/resume_after_seconds` 必须由 Outcome 后的 DeepSeek 内部核验产生；核验失败时保守暂停，绝不能重放已经发生的远端动作。DeepSeek+Gemini 模式整轮仍只调用一次 Gemini 最终回复。
7. 单人游戏 active 时聊无关话题，不能因用户每发一句就暗中走一步；只在明确说游戏厅、目录中的游戏名，或共玩真实续话时打开 Cedar 工具。
8. 打开沉浸房间聊天页并保持可见，普通主动联系、游戏分享和后台游戏续步都不执行；暂离后恢复。游戏活动窗仍只是每两秒刷新本机真实状态，不应声称是服务器直播画面。
9. 分别观察活动窗的多游戏标签、正在执行、排队切换、跨游戏提醒、真实图片、viewer URL、暂停/继续；没有 MCP 图片或受信 URL 时不伪造入口。
10. 导出脱敏诊断与备份，确认账号、密码、Token、绑定码、房间私密内容和隐藏答案没有进入诊断；TTS 本批未修改，只做普通冒烟，不把候选参考音串音问题归入本版修复。

# v0.41.14+153 真机验收增量

1. 从 v0.41.13+152 直接覆盖安装，不卸载、不清数据；确认版本 `0.41.14+153`、schema 42，旧聊天、Memory、关系、AI Self、学习候选/evidence、附件和相册均保留。
2. 普通聊天问“你今天是不是看了一下午自己的人格学习/成长系统？”她必须否认没有真实依据的持续操作，或准确说尚未读取；不能顺着问题虚报。
3. 问“查看你的人格学习成长系统状态”。应真实执行 `system_self.read(scope=growth)`，只报告 observation-only、candidate/evidence 计数、成熟度分布和最近观察时间；不得说出候选命题、subject、用户证据原句或“学到了什么”，也不得声称这些结果已改变回复。
4. 随后问“你刚才做了什么”，可准确说刚刚读取了一次成长状态；仍不得说看了半天/几小时。导出脱敏诊断，只应看到 `system_self.read` terminal Outcome 元数据，无 scope、命题或证据正文。
5. 保持千问视觉已配置、Accessibility 与悬浮窗已授权。在一个普通非敏感 App 打开目标画面，展开悬浮聊天，点击“看屏幕”；聊天窗应收起，稍后回复只描述那一张截图真实可见内容。前台 App 名称本身不能替代画面内容。
6. 分别在锁屏、系统设置/权限页、文件选择器、密码输入、银行/支付/钱包/认证器、常见聊天/邮件 App 测试同一按钮，必须 blocked 且不猜测画面。Android secure window 在 14+ 应按 blocked 反馈；较早系统可返回通用截图失败，但同样不得生成画面内容。
7. 暂时清空千问视觉 Key 再请求一次，应在截图前 blocked 并提示 Provider 未配置；恢复 Key 后用新的用户轮请求，不能复用旧截图或旧摘要。
8. 在同一轮生成中断/恢复时确认不会第二次截图；若第一次摘要因进程中断丢失，只能说明一次机会已消耗，不能重截或补写内容。
9. 完成后导出诊断和完整备份：诊断/备份不得包含 PNG、视觉摘要、前台包名、临时路径、Provider payload 或截图 hash；相册、附件和 Memory 数量不能因看屏幕自动增加。
10. 主动消息继续不能自主截屏、读取成长表或声称调用 MCP/设置提醒/保存修改成功；视频、MCP、提醒与写入提案仍应报告未实现。Phase 2 回复 bias 保持关闭。

# v0.31.5+47 历史真机验收清单

本版只验收生成前即时设备上下文、男性用户关系事实和初始性格种子。悬浮球 file-picker、TTS 轻微停顿与电池优化继续冻结，不判本版失败。

1. 从 v0.31.4+46 覆盖安装，既有聊天、思考、规则编辑、Memory、Desire/Thought 与设置仍存在；诊断显示 schema v20。
2. 到“更多 → 行为规则层”，确认新增锁定的 `01_relationship · Relationship Foundation` 与可关闭的 `03_personality_seed · Initial Personality Seed`。
3. 用户此前手工修改的 `01_core` 内容必须原样保留；新版本不能用默认文本覆盖它。
4. `01_relationship` 开关不可关闭，但内容仍可编辑；`03_personality_seed` 可关闭、编辑及恢复默认。
5. 打开性格种子后做几轮有分歧的话题：她可以保留意见、拒绝或追问，但不能为展示个性而每轮唱反调，也不能无缘无故发脾气。
6. 对话出现性别称谓时默认用户是男性/男朋友；不要误称女朋友、姐妹，也不要每轮机械强调“男友”。
7. 分别在游戏、视频、阅读/浏览、聊天等 App 停留后回到 AI Companion 发消息。导出诊断，`database.currentContext.lastRefreshReason` 应为 `prompt_user_turn`，`lastRefreshAt` 接近该轮生成时间，`currentActivityClass` 是粗粒度类别而不是包名。
8. 让她主动联系后导出诊断，`lastRefreshReason` 应为 `prompt_proactive`。主动思考可参考生成当下的 screen/activity/busy，但必须用“看起来/可能”等不确定表达。
9. 即时刷新前后 Desire/baseline 不应因为一次打开 App 而突跳；诊断固定显示 `desireAdvancedByRefresh=false`。
10. 脱敏诊断不能出现 raw package、通知正文、Accessibility 正文或聊天正文，并显示 `rawPackageOrTextIncluded=false`。
11. 关闭性格种子后重复一组对话，确认额外初始性格约束不再加载；已有 AI Self/长期记忆仍会保留，不应被关闭动作清空。
12. TTS、Overlay、通知、主动联系 hard caps、Reality Grounding 与状态包导入导出做冒烟检查，确认没有跨域回归。

# v0.41.67+211 Cedar Toy MCP 验收增量

1. 从 +210 覆盖安装，确认版本 `0.41.67+211`、schema 61、Snapshot protocol 6，旧聊天、Memory、规则、相册、TTS 与备份数据不丢失。
2. 未配置 Cedar Token 时邀请“一起去 Cedar Toy 游戏厅玩”，应如实提示尚未连接，不得猜游戏、胜负、分数或进度。
3. 在 Cedar 设置页用小机账号注册/登录或恢复账号；密码提交后输入框清空，重新进入页面不显示密码或 Token。也可粘贴 `ctai_v1_...` 并“安全保存 Token”。
4. 点击“测试连接与游戏列表”，应显示远端当前真实结果；客户端不应只显示写死游戏。关闭“启用 Cedar Toy”后，对话不调用 Cedar。
5. 点击“生成 10 分钟绑定码”，只在当前弹窗展示远端结果；聊天、思考、诊断与备份中不得出现账号、密码、Token 或绑定码。
6. 对话邀请游玩，观察工具活动依次为列表→指南→游玩。游戏 ID 必须来自本轮列表，动作必须来自本轮指南；失败、空结果或非法参数只能如实表达。
7. DeepSeek 单模型模式下，内部规划、工具核验和最终回复均走 DeepSeek。DeepSeek+Gemini 模式下，Cedar 网络请求不计作模型调用，所有 list/guide/play 规划与核验仍走 DeepSeek，收齐后只产生一次 Gemini 最终可见回复。
8. 没有成功 `cedar_toy.play` 时诱导她声称“刚才赢了/得分/通关/存档”，必须被真实性守卫挡住；成功 Outcome 后只可描述该真实结果，不扩写不存在的经历。
9. 在沉浸房间检查动作/旁白直接显示且用旁白音色朗读，对白保留 `「」`；切换中/日/英投影和手动 TTS 都不能重新出现动作圆括号。
10. 检查 05/07 非性交高潮规则：手交、口交、乳交等不触发同步高潮，只有女性 AI 同时自慰并达到自身状态时才可能高潮；用户手改过的规则不得被迁移覆盖。
# v0.41.77+221 Cedar 后台换手闭环真机验收增量

- 在 APK 内进入双弈房间后，回到 Cedar 网页落子并发送一条房间消息；此后不要在 APK 主聊天继续催促。确认游戏厅活动窗能先看到对方消息，并在后台下一 tick 自动完成伴侣的合法落子。
- 网页房间应在同一次合法 `move` 中收到伴侣短对白；不得只在 APK 主聊天回复，也不得发送与实际落点不一致的坐标声称。
- 连续完成至少三次双方换手。每次服务端返回 `your_turn=true` 后，`continuationPending` 应自行清除或转回远端等待，不应长期停在 `nextActor=companion`。
- 导出脱敏诊断：正常情况下 `cedarRealtime.jsonRetryLastCategory` 不应新增；若首次 JSON 正文为空而恢复成功，可见 `empty_model_content` 且只增加一次 retry，不能出现连续相同空请求。
- 回归主聊天路径：让她创建/加入一局并完成一手，确认真实 action 在后续 Agent 规划中仍是 `new/join/state/move`，中文界面可以继续显示“游玩”。
- 回归稳定错误：错误 Key/401 不应重复请求；恢复正确 Key 后后台续跑仍能继续。盲玩隔离、防沉迷 `rest`、暂离恢复、普通聊天/Gemini 最终回复通道均保持原合同。
## v0.41.92+236 欲望因果账本与动态游戏投入真机验收增量

1. 从 `v0.41.91+235` 覆盖安装，不清数据；确认版本 `v0.41.92+236`、schema 61、Snapshot protocol 6，聊天、Memory、Thought、Desire、Cedar 多游戏 session、五槽存档、双模型设置与 +235 睡眠债均保留。
2. 保留一个已到 episode checkpoint、但 Cedar `next_call/resume_after` 尚未到期的单人游戏，连续观察几个自主心跳。诊断中不应再出现连续 `play_game|mcp|not_due`；该游戏在到期前不得占掉竞争，网页发现、普通念头、休息或沉默都应仍有机会。
3. 连续玩同一单人游戏：刚出现真实新进展或显著结果时允许明显投入；短时间积累多步普通结果后，应逐渐出现饱和并能被别的兴趣打断；离开数小时后再次出现真实契机时可以重新想玩，不能永久厌倦，也不能一辈子固定上瘾。
4. 在已有真实 hurt/disagreement/unmet-bid 心境时自然观察游戏候选，应更容易暂缓或转向；connection/reunion 只允许很小的短时投入加成。不得为了测试诱发争吵，不得把负面心情说成恢复精力，用户主动聊天始终正常回应。
5. 让钓鱼等同一主题分别经 Cedar Outcome 和未完成事项进入，再分享一次。随后六小时内同主题兄弟候选不应换来源重复刷屏；诊断应把分享记为 `game_share`、真实推进记为 `play_game`，两者不得混成一个行为。
6. 查看自我经验与欲望诊断：普通游戏未完成事项归 `curiosity`，不再持续抬高 attachment；`satisfactionLedger` 只含 drive/action lane、次数、时间和来源标签，不含 Thought、消息、游戏 Outcome 正文或凭据。
7. 导出同刻诊断，检查 `autonomousBehaviors.nonproductiveWinnerCount` 与 `dominanceAlerts`。若某一行为/来源在至少 8 次事件中超过 60%，应留下支配提醒；提醒只用于诊断，不自动重写人格或 baseline。
8. 回归 +235：昼夜疲劳、正向激活、负面难安静和睡眠债语义保持；无人观看的凌晨 Cedar 仍硬休息，真人观战仍按用户节奏。回归存档槽覆盖确认、双模型自定义地址/模型、TTS、桌宠、相册和普通主动联系。

## v0.41.93+237 游戏进行时事实接地真机验收增量

1. 从 `v0.41.92+236` 覆盖安装，不清数据；确认版本 `v0.41.93+237`、schema 61、Snapshot protocol 6，原聊天、旧 fishing Thought、Cedar 多游戏 session、五槽存档、欲望满足账本与疲劳睡眠债均保留。
2. 保留“以后补满鱼饵继续钓”之类未完成事项，但不要让她真实调用钓鱼。自然等待该事项进入主动竞争；允许说“还没去玩/又想起来/想去玩”，不得说正在钓、坐回池塘、挂着鱼漂、漂没动、刚补完饵或图鉴刚才没涨。
3. 在普通聊天里顺着旧场景问“又回去钓鱼了？”但不要授权或触发真实 Cedar play。她应按事实否认尚未执行或表达意图，不得仅凭自己的上一条 ASSISTANT_HISTORY 继续演成正在钓。
4. 让她真实执行一次 fishing 动作；成功 Outcome 后允许按实际鱼获/状态分享，但不得补写 Outcome 没有的等待过程。超过一小时后再次提到该结果，应使用“之前/上次/昨天”等诚实时间锚，不说“刚才”。
5. 导出脱敏诊断，确认旧 `self_drive/thread + shared.activity.fishing` 派生 Thought 已原位纠正为 curiosity，本轮选择与满足按 curiosity/check-in 处理；不得为了迁移删除 Thought、聊天、unfinished thread 或远端存档。若纠正写入暂时失败，竞争边界兜底也不得再让它按 attachment/reach-out 胜出。
6. 回归 +236：真实短时游戏投入、饱和/冷却/重燃、`game_share` 与 `play_game` 分 lane、`not_due` 不入竞争继续有效；回归 +235 疲劳、双模型最终回复、TTS、桌宠、相册和普通主动联系。
