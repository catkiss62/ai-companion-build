diff --git a/README.md b/README.md
index 5c505fb..415ca1e 100644
--- a/README.md
+++ b/README.md
@@ -1,4 +1,4 @@
-# AI Companion · v0.28.3 TTS Packaging + Chat Startup Fix
+# AI Companion · v0.28.4 TTS Packaging + Chat Startup Fix
 
 Local-first Android AI companion prototype. The AI keeps an explicit AI identity, six-layer relationship rules, local long-term memory, Relationship/Desire/Thought continuity, proactive messaging, Android daily awareness, true overlay chat, local Meju Bert-VITS2 voice, durable generation/recovery and single-Active-Brain phone↔tablet continuity.
 
@@ -44,5 +44,5 @@ See:
 The v0.28.1 recovery surface exposed the next real-device blocker: Android sqflite rejects `PRAGMA journal_mode = WAL` through `execute()` because that PRAGMA returns data. v0.28.2 keeps the visible startup recovery and performs query-returning PRAGMAs through `rawQuery()`.
 
 
-## v0.28.3 real-device fixes
-The Android 15 checkpoint exposed two independent issues: AGP release stripping changed the user-validated `libbertvits2.so` fingerprint, and chat startup awaited best-effort long-running maintenance before exposing the message list. v0.28.3 preserves the six Meju native payloads during release packaging, fixes daily-continuity maintenance allowlisting, moves startup maintenance off the visible chat loading path, and adds a small on-device DeepSeek API connection probe.
+## v0.28.4 real-device fixes
+The Android 15 checkpoint exposed two independent issues: AGP release stripping changed the user-validated `libbertvits2.so` fingerprint, and chat startup awaited best-effort long-running maintenance before exposing the message list. v0.28.4 preserves the six Meju native payloads during release packaging, fixes daily-continuity maintenance allowlisting, moves startup maintenance off the visible chat loading path, and adds a small on-device DeepSeek API connection probe.
diff --git a/android/app/src/main/kotlin/com/aicompanion/localfirst/LegacyTtsRuntime.kt b/android/app/src/main/kotlin/com/aicompanion/localfirst/LegacyTtsRuntime.kt
index 61403e6..0ed57ba 100644
--- a/android/app/src/main/kotlin/com/aicompanion/localfirst/LegacyTtsRuntime.kt
+++ b/android/app/src/main/kotlin/com/aicompanion/localfirst/LegacyTtsRuntime.kt
@@ -5,12 +5,10 @@ import dalvik.system.DexClassLoader
 import java.io.File
 import java.lang.reflect.InvocationTargetException
 import java.lang.reflect.Method
+import java.lang.reflect.Proxy
 import java.util.concurrent.CountDownLatch
 import java.util.concurrent.TimeUnit
 import java.util.concurrent.atomic.AtomicReference
-import kotlin.coroutines.Continuation
-import kotlin.coroutines.EmptyCoroutineContext
-import kotlin.coroutines.intrinsics.COROUTINE_SUSPENDED
 
 /**
  * Compatibility adapter for the already-working LocalTTSEngine shipped in the
@@ -68,8 +66,12 @@ class LegacyTtsRuntime(private val context: Context) {
                 .newInstance(appContext)
                 .also { engine = it }
 
-            val initialize = clazz.getMethod("initialize", Continuation::class.java)
-            invokeSuspendBlocking(instance, initialize, timeoutSeconds = 180)
+            val initialize = findLegacySuspendMethod(
+                clazz = clazz,
+                name = "initialize",
+                leadingParameterTypes = emptyList(),
+            )
+            invokeLegacySuspendBlocking(instance, initialize, timeoutSeconds = 180)
 
             val ready = clazz.getMethod("isReady").invoke(instance) as? Boolean ?: false
             if (!ready) error("legacy LocalTTSEngine finished initialize() but isReady=false")
@@ -95,12 +97,12 @@ class LegacyTtsRuntime(private val context: Context) {
         }
         val instance = engine ?: error("legacy TTS engine is unavailable")
         return runCatching {
-            val method = instance.javaClass.getMethod(
-                "generateTTS",
-                String::class.java,
-                Continuation::class.java,
+            val method = findLegacySuspendMethod(
+                clazz = instance.javaClass,
+                name = "generateTTS",
+                leadingParameterTypes = listOf(String::class.java),
             )
-            val value = invokeSuspendBlocking(
+            val value = invokeLegacySuspendBlocking(
                 instance,
                 method,
                 text,
@@ -203,19 +205,97 @@ class LegacyTtsRuntime(private val context: Context) {
         )
     }
 
-    private fun invokeSuspendBlocking(
+    /**
+     * Resolve suspend methods using the legacy runtime's own Continuation
+     * parameter type. The packaged Meju dex contains its own un-obfuscated
+     * kotlin.coroutines classes, while the host Flutter release may R8-rename
+     * its Kotlin Continuation (for example to WO.d). Passing the host class to
+     * Class.getMethod therefore produces NoSuchMethodException even though the
+     * method exists.
+     */
+    private fun findLegacySuspendMethod(
+        clazz: Class<*>,
+        name: String,
+        leadingParameterTypes: List<Class<*>>,
+    ): Method {
+        val candidates = clazz.methods.filter { method ->
+            if (method.name != name) return@filter false
+            val params = method.parameterTypes
+            if (params.size != leadingParameterTypes.size + 1) return@filter false
+            if (params.lastOrNull()?.name != LEGACY_CONTINUATION_CLASS) return@filter false
+            leadingParameterTypes.indices.all { index ->
+                params[index] == leadingParameterTypes[index]
+            }
+        }
+        if (candidates.size != 1) {
+            val signatures = clazz.methods
+                .filter { it.name == name }
+                .joinToString("; ") { method ->
+                    method.parameterTypes.joinToString(
+                        prefix = "${method.name}(",
+                        postfix = ")",
+                    ) { it.name }
+                }
+            error(
+                "Unable to resolve legacy suspend $name; " +
+                    "matches=${candidates.size}; available=$signatures",
+            )
+        }
+        return candidates.single()
+    }
+
+    private data class LegacySuspendOutcome(
+        val value: Any? = null,
+        val error: Throwable? = null,
+    )
+
+    private fun invokeLegacySuspendBlocking(
         target: Any,
         method: Method,
         vararg args: Any?,
         timeoutSeconds: Long,
     ): Any? {
+        val continuationType = method.parameterTypes.lastOrNull()
+            ?: error("Legacy suspend method ${method.name} has no continuation parameter")
+        if (continuationType.name != LEGACY_CONTINUATION_CLASS || !continuationType.isInterface) {
+            error(
+                "Unexpected continuation type for ${method.name}: " +
+                    "${continuationType.name}",
+            )
+        }
+
+        val legacyLoader = continuationType.classLoader
+            ?: target.javaClass.classLoader
+            ?: error("Legacy TTS class loader is unavailable")
+        val emptyContext = Class.forName(
+            LEGACY_EMPTY_CONTEXT_CLASS,
+            true,
+            legacyLoader,
+        ).getField("INSTANCE").get(null)
+        val suspendedMarker = Class.forName(
+            LEGACY_INTRINSICS_CLASS,
+            true,
+            legacyLoader,
+        ).getMethod("getCOROUTINE_SUSPENDED").invoke(null)
+
         val latch = CountDownLatch(1)
-        val outcome = AtomicReference<Result<Any?>?>()
-        val continuation = object : Continuation<Any?> {
-            override val context = EmptyCoroutineContext
-            override fun resumeWith(result: Result<Any?>) {
-                outcome.compareAndSet(null, result)
-                latch.countDown()
+        val outcome = AtomicReference<LegacySuspendOutcome?>()
+        val continuation = Proxy.newProxyInstance(
+            legacyLoader,
+            arrayOf(continuationType),
+        ) { proxy, called, callArgs ->
+            when (called.name) {
+                "getContext" -> emptyContext
+                "resumeWith" -> {
+                    val raw = callArgs?.getOrNull(0)
+                    outcome.compareAndSet(null, decodeLegacyResult(raw))
+                    latch.countDown()
+                    null
+                }
+                "toString" -> "LegacyContinuationProxy(${method.name})"
+                "hashCode" -> System.identityHashCode(proxy)
+                "equals" -> proxy === callArgs?.getOrNull(0)
+                else -> error("Unexpected legacy Continuation method: ${called.name}")
             }
         }
 
@@ -225,11 +305,27 @@ class LegacyTtsRuntime(private val context: Context) {
             throw e.targetException ?: e
         }
 
-        if (immediate !== COROUTINE_SUSPENDED) return immediate
+        if (immediate !== suspendedMarker) return immediate
         if (!latch.await(timeoutSeconds, TimeUnit.SECONDS)) {
             error("Timed out waiting for ${method.name} (${timeoutSeconds}s)")
         }
-        return outcome.get()?.getOrThrow()
+        val completed = outcome.get()
+            ?: error("Legacy ${method.name} resumed without an outcome")
+        completed.error?.let { throw it }
+        return completed.value
+    }
+
+    private fun decodeLegacyResult(raw: Any?): LegacySuspendOutcome {
+        if (raw == null) return LegacySuspendOutcome(value = null)
+        if (raw.javaClass.name == LEGACY_RESULT_FAILURE_CLASS) {
+            val failure = runCatching {
+                raw.javaClass.getDeclaredField("exception").apply { isAccessible = true }.get(raw)
+            }.getOrNull() as? Throwable
+            return LegacySuspendOutcome(
+                error = failure ?: IllegalStateException("Legacy coroutine failed"),
+            )
+        }
+        return LegacySuspendOutcome(value = raw)
     }
 
     private fun rootMessage(error: Throwable): String {
@@ -245,5 +341,9 @@ class LegacyTtsRuntime(private val context: Context) {
             "com.gamedeveloper.urbanfriendshipstory.tts.LocalTTSEngine"
         private const val RUNTIME_ASSET_DIR = "legacy_tts/runtime"
         private const val RUNTIME_CACHE_DIR = "legacy_tts_v25_63a8c10f"
+        private const val LEGACY_CONTINUATION_CLASS = "kotlin.coroutines.Continuation"
+        private const val LEGACY_EMPTY_CONTEXT_CLASS = "kotlin.coroutines.EmptyCoroutineContext"
+        private const val LEGACY_INTRINSICS_CLASS = "kotlin.coroutines.intrinsics.IntrinsicsKt"
+        private const val LEGACY_RESULT_FAILURE_CLASS = "kotlin.Result\$Failure"
     }
 }
diff --git a/docs/DEV_STATUS.md b/docs/DEV_STATUS.md
index fdec742..c13268e 100644
--- a/docs/DEV_STATUS.md
+++ b/docs/DEV_STATUS.md
@@ -1,18 +1,18 @@
-# v0.28.3 开发状态 · TTS Packaging + Chat Startup Fix
+# v0.28.4 开发状态 · TTS Packaging + Chat Startup Fix
 
 ## 真机暴露
 - v0.28.2 已成功进入完整 App，Android 权限与系统页面可用。
 - 深度自检只在 `libbertvits2.so` 报 golden fingerprint mismatch。对已构建 release APK 复核确认：源码/黄金库为 710848 bytes，而 release APK 中被 AGP strip 成 635352 bytes；其它五个核心 Meju native 库保持字节一致。
 - 聊天页持续加载并不等于 DeepSeek 连接失败。源码审计发现 ChatController 在 `loading=false` 之前同步等待 long-running maintenance；同时该维护任务会清理 `daily_continuity(window_start)`，但数据库维护 allowlist 漏掉该 table/column，必然抛 `Unsupported maintenance table/column`。真机诊断中的 `hasMaintenanceError=true` 与此吻合。
 
-## v0.28.3 修复
+## v0.28.4 修复
 - AGP `packaging.jniLibs.keepDebugSymbols` 明确保留 6 个 Meju 黄金 native payload，尤其避免 `libbertvits2.so` 在 release 中被 strip。
 - GitHub build 在上传 Artifact 前逐项比较 APK 内 6 个 native 库与源码黄金库 SHA-256/size，不一致直接失败。
 - `daily_continuity` + `window_start` 纳入长期维护 allowlist。
 - Chat 先读取消息并结束可见 loading，再异步执行 best-effort maintenance；任何维护异常不能再造成永久转圈。
 - 设置页增加“测试 API 连接”，用当前 endpoint/Key/model 做 30 秒受控探针，不写入聊天与长期记忆。
 - 数据库 schema 保持 v18；Memory/Relationship/Transfer/TTS 模型与运行时内容不改。
-- 版本更新为 `0.28.3+31`。
+- 版本更新为 `0.28.4+32`。
 
 ---
 
diff --git a/docs/ROADMAP.md b/docs/ROADMAP.md
index c0e665e..c5393bf 100644
--- a/docs/ROADMAP.md
+++ b/docs/ROADMAP.md
@@ -1,7 +1,7 @@
-# Roadmap after v0.28.3
+# Roadmap after v0.28.4
 
 ## Immediate checkpoint
-First confirm the two real-device blockers fixed in v0.28.3: release-packaged Meju native fingerprints and chat initialization. Use the new API probe to separate DeepSeek connectivity from local maintenance/UI issues. After TTS + chat both pass, freeze the runtime baseline and move UI polish into a dedicated stage rather than mixing it with core debugging.
+First confirm the two real-device blockers fixed in v0.28.4: release-packaged Meju native fingerprints and chat initialization. Use the new API probe to separate DeepSeek connectivity from local maintenance/UI issues. After TTS + chat both pass, freeze the runtime baseline and move UI polish into a dedicated stage rather than mixing it with core debugging.
 
 ---
 
diff --git a/docs/TEST_CHECKLIST.md b/docs/TEST_CHECKLIST.md
index fe48386..383ab8e 100644
--- a/docs/TEST_CHECKLIST.md
+++ b/docs/TEST_CHECKLIST.md
@@ -1,4 +1,4 @@
-# v0.28.3 真机检查
+# v0.28.4 真机检查
 
 1. 启动进入主界面，不再卡 Chat loading spinner。
 2. “更多 → AI 与陪伴设置 → 测试 API 连接”：先确认当前 API 地址/Key/模型可用。
diff --git a/docs/TTS_LEGACY_COROUTINE_BRIDGE_v0.28.4.md b/docs/TTS_LEGACY_COROUTINE_BRIDGE_v0.28.4.md
new file mode 100644
index 0000000..55627f8
--- /dev/null
+++ b/docs/TTS_LEGACY_COROUTINE_BRIDGE_v0.28.4.md
@@ -0,0 +1,7 @@
+# v0.28.4 Legacy TTS coroutine bridge
+
+Real-device v0.28.3 proved the 37/37 Meju payload integrity check passes, but `LocalTTSEngine.initialize()` failed with `NoSuchMethodException ... [interface WO.d]`.
+
+The cause is class-loader/type identity, not a missing TTS method: the Meju dex method descriptor is `initialize(kotlin.coroutines.Continuation)`, while the host release may R8-rename its own Kotlin Continuation. The bridge previously looked up the method using the host Continuation class.
+
+v0.28.4 resolves the legacy suspend methods from the loaded Meju class itself and creates a dynamic Continuation proxy implementing the exact interface from the legacy class loader. The same path is used for both `initialize()` and `generateTTS()`.
diff --git a/lib/core/diagnostics/preflight_diagnostics.dart b/lib/core/diagnostics/preflight_diagnostics.dart
index 77434ac..7f9118e 100644
--- a/lib/core/diagnostics/preflight_diagnostics.dart
+++ b/lib/core/diagnostics/preflight_diagnostics.dart
@@ -341,7 +341,7 @@ class PreflightDiagnosticsService {
     final file = File(p.join(temp.path, 'ai_companion_diagnostics_$stamp.txt'));
     final encoder = const JsonEncoder.withIndent('  ');
     final text = StringBuffer()
-      ..writeln('AI Companion v0.28.3 · REDACTED LOCAL DIAGNOSTIC REPORT')
+      ..writeln('AI Companion v0.28.4 · REDACTED LOCAL DIAGNOSTIC REPORT')
       ..writeln('This report intentionally excludes relationship/chat/reference plaintext and API secrets.')
       ..writeln()
       ..writeln(encoder.convert(snapshot.report));
diff --git a/lib/features/inner/inner_page.dart b/lib/features/inner/inner_page.dart
index 9e9c554..91c42e9 100644
--- a/lib/features/inner/inner_page.dart
+++ b/lib/features/inner/inner_page.dart
@@ -193,7 +193,7 @@ class _InnerPageState extends State<InnerPage> {
     return ListView(
       padding: const EdgeInsets.all(16),
       children: [
-        Text('她的内心 · v0.28.3 诊断', style: Theme.of(context).textTheme.headlineSmall),
+        Text('她的内心 · v0.28.4 诊断', style: Theme.of(context).textTheme.headlineSmall),
         const SizedBox(height: 4),
         const Text('当前保留数值与数据库状态方便调试；正式视觉层以后可以隐藏这些工程细节。'),
         const SizedBox(height: 16),
diff --git a/lib/features/more/companion_more_page.dart b/lib/features/more/companion_more_page.dart
index 806473d..d7e708a 100644
--- a/lib/features/more/companion_more_page.dart
+++ b/lib/features/more/companion_more_page.dart
@@ -96,7 +96,7 @@ class CompanionMorePage extends StatelessWidget {
         const SizedBox(height: 8),
         Center(
           child: Text(
-            'AI Companion · v0.28.3',
+            'AI Companion · v0.28.4',
             style: Theme.of(context).textTheme.bodySmall?.copyWith(
                   color: Theme.of(context).colorScheme.onSurfaceVariant,
                 ),
diff --git a/lib/features/system/real_device_checkpoint_page.dart b/lib/features/system/real_device_checkpoint_page.dart
index 47e97b9..2bba785 100644
--- a/lib/features/system/real_device_checkpoint_page.dart
+++ b/lib/features/system/real_device_checkpoint_page.dart
@@ -126,7 +126,7 @@ class _RealDeviceCheckpointPageState extends State<RealDeviceCheckpointPage> {
         Text('第一次综合真机验收', style: Theme.of(context).textTheme.headlineSmall),
         const SizedBox(height: 6),
         Text(
-          '这不是新的功能页面，而是 v0.28.3 Checkpoint 的测试顺序。先确认单机底层，再测试双设备顶号；任何一项失败都优先保存脱敏诊断报告，不需要盲试多个 APK。',
+          '这不是新的功能页面，而是 v0.28.4 Checkpoint 的测试顺序。先确认单机底层，再测试双设备顶号；任何一项失败都优先保存脱敏诊断报告，不需要盲试多个 APK。',
           style: Theme.of(context).textTheme.bodyMedium?.copyWith(
                 color: Theme.of(context).colorScheme.onSurfaceVariant,
               ),
diff --git a/lib/main.dart b/lib/main.dart
index 35b925a..aa6e80a 100644
--- a/lib/main.dart
+++ b/lib/main.dart
@@ -148,7 +148,7 @@ class _StartupRecoveryRootState extends State<_StartupRecoveryRoot> {
                   ),
                   const SizedBox(height: 6),
                   Text(
-                    'v0.28.3 · TTS / Chat Recovery',
+                    'v0.28.4 · TTS / Chat Recovery',
                     textAlign: TextAlign.center,
                     style: Theme.of(context).textTheme.bodyMedium,
                   ),
diff --git a/pubspec.yaml b/pubspec.yaml
index 9449827..70bcf6b 100644
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@ -1,7 +1,7 @@
 name: ai_companion_localfirst
 description: Local-first Android AI companion prototype with DeepSeek reasoning, local memory, overlay chat, device perception and Nearby transfer.
 publish_to: 'none'
-version: 0.28.3+31
+version: 0.28.4+32
 
 environment:
   sdk: '>=3.12.0 <4.0.0'
diff --git a/tools/validate_v0284_legacy_coroutine.py b/tools/validate_v0284_legacy_coroutine.py
new file mode 100755
index 0000000..ab15559
--- /dev/null
+++ b/tools/validate_v0284_legacy_coroutine.py
@@ -0,0 +1,83 @@
+#!/usr/bin/env python3
+from __future__ import annotations
+
+import struct
+import zipfile
+from pathlib import Path
+
+ROOT = Path(__file__).resolve().parents[1]
+RUNTIME = ROOT / 'android/app/src/main/assets/legacy_tts/runtime/runtime_06.jar'
+SOURCE = ROOT / 'android/app/src/main/kotlin/com/aicompanion/localfirst/LegacyTtsRuntime.kt'
+ENGINE = 'Lcom/gamedeveloper/urbanfriendshipstory/tts/LocalTTSEngine;'
+CONT = 'Lkotlin/coroutines/Continuation;'
+
+
+def dex_engine_methods(data: bytes) -> dict[str, list[list[str]]]:
+    u4 = lambda o: struct.unpack_from('<I', data, o)[0]
+    u2 = lambda o: struct.unpack_from('<H', data, o)[0]
+    string_ids_size, string_ids_off = u4(56), u4(60)
+    type_ids_size, type_ids_off = u4(64), u4(68)
+    proto_ids_size, proto_ids_off = u4(72), u4(76)
+    method_ids_size, method_ids_off = u4(88), u4(92)
+
+    def uleb(off: int) -> tuple[int, int]:
+        result = 0
+        shift = 0
+        while True:
+            b = data[off]
+            off += 1
+            result |= (b & 0x7F) << shift
+            if b < 0x80:
+                return result, off
+            shift += 7
+
+    def get_string(i: int) -> str:
+        off = u4(string_ids_off + 4 * i)
+        _, off = uleb(off)
+        end = data.index(b'\0', off)
+        return data[off:end].decode('utf-8', 'replace')
+
+    types = [get_string(u4(type_ids_off + 4 * i)) for i in range(type_ids_size)]
+    protos: list[list[str]] = []
+    for i in range(proto_ids_size):
+        _, _, params_off = struct.unpack_from('<III', data, proto_ids_off + 12 * i)
+        params: list[str] = []
+        if params_off:
+            count = u4(params_off)
+            params = [types[u2(params_off + 4 + 2 * j)] for j in range(count)]
+        protos.append(params)
+
+    found: dict[str, list[list[str]]] = {}
+    for i in range(method_ids_size):
+        class_idx, proto_idx, name_idx = struct.unpack_from('<HHI', data, method_ids_off + 8 * i)
+        if types[class_idx] != ENGINE:
+            continue
+        found.setdefault(get_string(name_idx), []).append(protos[proto_idx])
+    return found
+
+
+def main() -> int:
+    with zipfile.ZipFile(RUNTIME) as zf:
+        methods = dex_engine_methods(zf.read('classes.dex'))
+    assert [CONT] in methods.get('initialize', []), methods.get('initialize')
+    assert ['Ljava/lang/String;', CONT] in methods.get('generateTTS', []), methods.get('generateTTS')
+
+    source = SOURCE.read_text(encoding='utf-8')
+    assert 'Continuation::class.java' not in source
+    assert 'import kotlin.coroutines.' not in source
+    for required in [
+        'findLegacySuspendMethod',
+        'Proxy.newProxyInstance',
+        'LEGACY_CONTINUATION_CLASS',
+        'LEGACY_EMPTY_CONTEXT_CLASS',
+        'LEGACY_INTRINSICS_CLASS',
+        'decodeLegacyResult',
+    ]:
+        assert required in source, required
+
+    print('v0.28.4 legacy coroutine bridge validation passed.')
+    return 0
+
+
+if __name__ == '__main__':
+    raise SystemExit(main())
